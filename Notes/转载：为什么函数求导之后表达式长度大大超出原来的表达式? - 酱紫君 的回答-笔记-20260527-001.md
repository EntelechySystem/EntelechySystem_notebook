---
title: 转载：为什么函数求导之后表达式长度大大超出原来的表达式? - 酱紫君 的回答-笔记-20260527-001
source: https://www.zhihu.com/question/609058716/answer/1954152484693058426
author:
  - "酱紫君​​新知答主已关注"
  - "hhh​侵略理论一律不信并反击，某些人为何我要怼就是因为他先和我作对​ 关注"
  - "TreborArcahv.​ 关注"
published:
created: 2026-01-07
description: 这个现象叫 Expression Swell，曾经搞的符号主义焦头烂额，符号求导被认为劣于自动求导。后来符号主义回…
tags:
  - clippings
  - 内容/符号计算
  - 来源/转载
  - 类型/笔记
---
# 转载：为什么函数求导之后表达式长度大大超出原来的表达式? - 酱紫君 的回答-笔记-20260527-001
[查看全部 28 个回答](https://www.zhihu.com/question/609058716)

创作声明：包含投资理财内容

2311 人赞同了该回答

![](https://picx.zhimg.com/50/v2-80e746a3320a15e54aaf777e55ebd940_720w.jpg?source=2c26e567)

这个现象叫 Expression Swell ，曾经搞的 符号主义 焦头烂额，符号求导被认为劣于 自动求导 。 后来符号主义回过味来了，你们™选的表征有问题啊！！！ 我符号计算一生不弱于人！ Tree 表征 Macsyma、Reduce 等早期的 CAS 清一色都认为表达式是树，是 AST。 一个数学表达式被认为是一个有根有序的树 T = ( N, Σ root children ) 是节点的集合 是一个符号字母表，包含变量、常量和操作符 每个节点 n ∈ 被赋予一个标签 label 如此一来，表达式 E 1 + sin ⁡ x ⋅ 在 树表征 下就可以记为 其根节点为一个乘法操作符 · ，该节点的两个子节点在结构上是完全相同的子树 在此子树中，任何对子表达式 的引用，都对应于一个独立且不被共享的子树 表达式树 的大小，记为 | ，定义为其节点的总数。 在这种表征下，符号微分算子 D 的行为就可以被形式化为树的变换规则。 微分算子 是一个从表达式树集合到表达式树集合的映射，: → 其定义基于一系列重写规则： u v 这里， 表示对子树 递归应用微分变换的结果。 现在我们就可以精确地定位表达式膨胀的发生机制。 考虑乘法法则的变换： 在生成的结果树中，原始的子树 和 被复制了。 新的树包含了两个独立的副本，一个 和一个 ，以及一个 如果 自身就很大，或者它们内部共享了更深层次的子表达式，这种复制行为将导致节点数量的指数增加。 设 表示树 的节点数。在不考虑任何代数化简的情况下，应用乘法法则的微分变换，其结果树的大小满足： ≥ 的规模不小于 ，那么结果树的规模将至少是原始子树规模总和的两倍。 对于复合函数高阶导数，这种效应会迭代地发生，导致规模的指数级增长。 On the Equivalence of Automatic and Symbolic Differentiation \[1\] 一文指出，不能将这种由树表征的复制行为所导致的规模膨胀，错误地归咎于符号微分这一数学概念本身。 DAG 表征 为了克服表达式树的内在冗余，需要一种能够共享公共子表达式的表征。 这种表征在图论中对应于有向无环图（Directed Acyclic Graph, DAG）。 一个表达式 的 DAG 表征是一个有向无环图 G V A 是顶点的集合，每个顶点 对应一个唯一的子表达式。 是有向边的集合。 一条从 到 的边 表示 的计算直接依赖于 的计算结果。 每个顶点 ，表示该顶点的操作。 入度为 0 的顶点被称为输入顶点，通常对应于变量或常量。 图中存在一个唯一的、出度为 0 的顶点，称为输出顶点，代表整个表达式 的值。 与树不同，DAG 中的任何一个顶点都可以被多个父节点引用。 这种情况下，表达式 的 DAG 表征中，子表达式 仅对应于一个顶点，该顶点同时被作为乘法操作顶点的两个输入。 简单来说，共享机制从根本上消除了语法上的冗余。 SLP 表达式 但是图这个东西吧，看起来很直观，写起来就很不太方便了。 因此在计算理论中，一般使用其等价的文本形式，也就是直线程序（Straight-Line Program, SLP）。 一个 SLP 是一个有限的指令序列 P I 2 … m ，其中每条指令 k 的形式为： ← ϕ j 是一个基本操作符， i 是先前已定义的变量，序列的最后一个变量 被指定为程序的输出。 SLP 与 DAG 存在一一对应的关系。 SLP 中的每条指令 对应 DAG 中的一个顶点 ，指令中的依赖关系 对应 DAG 中的有向边 写了这么一大坨，说白了其实就是引入变量和指针。 接下来，我们要在 SLP 表征 上重新定义符号微分算子 令 为计算函数 f 的 SLP。 我们希望计算其关于某个输入变量 的偏导数 ∂ 我们不妨引入一个新的变量集 ˙ 来表示中间变量 关于 给定计算 p 的指令，其对应的导数 由多元链式法则确定： ∑ l 此规则生成了新的 SLP 指令。 通过对原 SLP 的每条指令应用此规则，我们便可以构建出计算所有 的新 SLP。 众所周知 是一个求导会导致严重膨胀的函数。 这里使用 SLP 重新表征： (输入变量) exp ln (计算 3 根据 SLP 链式求导法则： --- 原始函数定义 --- --- 函数求导结果 --- − 其输出为 ，这个导数的 SLP 表征是紧凑且无冗余的。 它的规模与原函数 SLP 的规模呈线性关系，表达式膨胀在表示层面被完全消除了。 用编程里的说法，这里任何对 或 的引用都只是一个指针，并没有非结构性复制。 Unfolding Tree 虽然 SLP 提供了紧凑的表示，但我们还是需要一个人类可读的、单一的展开式表达式。 将一个 SLP 转换回展开式，等价于将其对应的 DAG 展开成一棵树，这个过程被称为展开（Unfolding）。 的展开操作 U 是一个递归过程，它将 中的所有中间变量 < 替换为它们各自的定义式，直至只剩下输入变量。 之所有有的会膨胀，有的不会膨胀。 根本原因在于在某些代数结构中，有时展开可以触发化简，从而得到比 SLP 更简洁的形式。 比如如下 SLP： a b 展开 得到 ，可化简为 最终形式反倒是比原 SLP 更简洁。 但是在绝大多数情况下，特别是对于微分产生的结果，中间变量之间不存在简单的代数消元关系。 此时，展开 SLP 会将共享的 DAG 结构强行复制成冗余的树结构，从而引发表达式膨胀现象。 还是考虑 的 SLP 表征： 如果对其暴力展开， 的表达式将被完整地代入到 的表达式中，而 的表达式又会被代入 中，如此反复迭代。 最终导致一个巨大的相似表达式： ′ log 所有的 CTE 部分都被全部复制再复制，此时再代入数值计算当然会爆炸。 Dual Number 那自动微分（Forward Mode AD）是怎么解决这个问题的呢？ 前向模式自动微分的工作原理是为每个中间变量 计算一个对偶数，也就是元组 ⟨ ⟩ Dual Number 表征下，计算 的操作在 AD 中的传播规则为： 可以看出，这个计算过程与上文一般导数 SLP 链式法则所执行的计算完全相同。 所以自动微分的数值优越性可以解释为： AD 可以在一个特定的输入点上，直接计算出导数的数值，而不必构造显式的导函数结构。 但是其实 SD 也是一样的，计算机内部干嘛要展开，用 DAG 表达不香吗，以 DAG 形式代入数值，一样不会有数值误差累积。 一文对此有详细的证明，自动微分并不存在什么计算优越性。 E-Graph 虽然 DAG 表征成功地解决了语法冗余（Syntactic Redundancy）问题。 然而导数计算效率的关键其实是在化简，这是一个语义等价（Semantic Equivalence）问题。 不同的数学表达式形式不同但意义相同： ≡ cos ¬ ∧ B ∨ DAG 对此无能为力，在 DAG 中， 是两个完全不同、不相关的子图。 传统的计算机 CAS 系统会尝试通过启发式的 simplify() 函数来解决此问题，子图匹配、子树匹配等方法非常不稳定且效率低下。 最要命的是 TRS 压根就不完备，所以 Mathematica 才会有修不完的化简 bug，压根没法 debug，只能人工排序。 所以为了从根本上解决语义等价问题，我们急需一种新的表征，它不仅能表示计算的依赖关系，还能内在地表示和维护表达式之间的等价关系。 这种表征就是等价图（Equivalence Graph, E-Graph） \[2\] E-Graph 是一种能够紧凑地表示大量等价表达式的图结构。 一个 E-Graph C 本质上由两部分组成，一个是并查集（Union-Find），另一个是典范哈希（Hashcons）。 不过这个太数学了，用的时候会将具体结构分为 E-Class 和 E-Node。 E-Class 代表一个等价类，它是一个集合，包含了所有已知等价的 E-Node。 在并查集 中，每个 E-Class 对应一个集合的代表元，也就是表达式树的根节点。 E-Node 代表一个表达式的顶层结构，一个 E-Node 的形式为 ，其中 是一个操作符，其操作数是 E-Class 的标识符。 E-Graph 的关键在于，它将所有等价的 E-Node 聚合在同一个 E-Class 中，从而使得对该等价类的任何引用，都同时隐含了其所有等价的表示形式。 在 E-Graph 的世界里，微分法则没什么特殊的，只是平平无奇的重写规则集 R 的一部分： ⇒ 同时规则集 还包含了大量的代数化简规则： 0 y z θ 这些都是非常普通而普适的，没那么多肮脏的人体工程排序，整个过程完备可控。 工程上来讲，E-Graph 最关键的特性在于并发搜索。 传统的流程是串行的，必须按照 求导 -> 膨胀 -> 化简 的顺序跑一遍，还很难复用。 而 E-Graph 完全打破了这样的限制，整个流程是并发的。 当应用一条微分规则 后，新产生的结构立即就会被加入到 E-Graph 中。 但与此同时，代数化简规则也在不断地并发运行。 恰好可以被化简为 ，那么规则 会立刻被包含，从而从根本上抑制表达式膨胀的发生。 整个饱和计算过程将求导、化简、代数变换全部统一，从一系列离散且顺序敏感的步骤，融合成了一个统一的并发寻找不动点的过程。 最重要的是，问题完备以后 AI 就可以发力了，TRS 那种漏洞百出的系统 AI 容易走偏，这已经成为限制 Mathematica，Rubi 等系统性能提升的最大障碍了。 而在 EGG 这种完备系统上就和下围棋一样，不会有无穷无尽的空子钻，可以放手让 AI 可以自我进化。 符号求导不是垃圾，自动微分也不是银弹。 符号主义的核心思想加上联结主义的最优化能力，这才是未来有脑子的 AI 该干的事。 参考 ^ https://arxiv.org/abs/1904.02990 A New Paradigm in Relational Query Rewrite and Beyond https://arxiv.org/abs/2407.12794

还没有人送礼物，鼓励一下作者吧

[发布于 2025-09-24 11:57](https://www.zhihu.com/question/609058716/answer/1954152484693058426) ・上海

