---
title: scene-suite包名迁移更新计划-计划-20260814-001
authors: Ethan Lin
year: 2026-08-14
tags:
  - 类型/计划
  - 类型/运作
  - 运作/计划
  - 日期/2026-08-14
  - 项目/EntelechySystem
  - 任务/scene-suite包名迁移
aliases:
  - scene-suite 包名迁移更新计划
---

# scene-suite 包名迁移更新计划（待审阅）

> [!attention]
> 本文件为**计划文档**，仅供审阅，尚未执行任何文件修改。

---

## 1. 背景与信件来源

经 mailbox/relay 多项目联动治理体系核查，`EntelechySystem` 已接入治理体系（`docs/mailbox/relay/`），并收到来自 **`scene-kit`**（原 `world-model-kit`）的投递信件：

- 信件位置：`EntelechySystem/docs/mailbox/relay/in-up/scene-kit.md`
- 信件类型：`out-down` / `support_advice`（支持建议）
- 优先级：high
- 创建时间：2026-08-12
- 终局状态：2026-08-13 已标记迁移就绪（v0.5.1）

同批投递对象：`EntelechySystem`、`ComplexSystemLab`、`yeegames`（均位于 `scene-kit/docs/mailbox/relay/out-down/`）。

## 2. 信件内容理解

### 2.1 品牌整合结论

`world-model-kit` 经历品牌整合，SDK 最终定名 **`scene-kit`**，伞形品牌为 **Scene Suite**，应用仓库为 **scene-studio**（闭源，与本迁移无关）。

| 旧名 | 新名 |
|------|------|
| `world-model-kit`（pip/pyproject） | `scene-kit` |
| `world_model_kit`（Python import） | `scene_kit` |
| 本地路径 `../world-model-kit` | `../scene-kit` |

### 2.2 关键承诺

- **公共 API 签名不变**（`WorldModel`、`EntityKind`、`WorldSnapshot` 等），无需改源码调用。
- 仓库目录已从 `world-model-kit` 移动到 `scene-kit`（`/Users/ethan/CoreFiles/ProjectsFile/` 下）。
- 迁移验证：pytest 39/39 通过，`import scene_kit` 可用。

### 2.3 信件指示的更新位置（ES 侧）

1. `pyproject.toml`：依赖声明 + `[tool.uv.sources]`
2. `docs/开发日志.md`：环境迁移条目（历史条目保留原名）
3. `docs/开发者指南.md`：依赖说明
4. `README.md`：依赖描述
5. `CHANGELOG.md`：历史条目保留原名；新增条目标注改名

## 3. 已核实的实际需更新文件清单（EntelechySystem）

> 已逐一核查工作区现状，确认以下文件存在旧名引用：

| # | 文件 | 行 | 现状 | 改法 |
|---|------|-----|------|------|
| 1 | `pyproject.toml` | L12 | `"world-model-kit>=0.2.0",` | → `"scene-kit>=0.2.0",` |
| 2 | `pyproject.toml` | L21 | `world-model-kit = { path = "../world-model-kit", editable = true }` | → `scene-kit = { path = "../scene-kit", editable = true }` |
| 3 | `scripts/uv_migrate.sh` | L12 | 注释 `本地依赖（RECS / world-model-kit）` | → `RECS / scene-kit` |
| 4 | `scripts/uv_migrate.sh` | L119 | `check_import world_model_kit` | → `check_import scene_kit` |
| 5 | `docs/开发者指南.md` | L26 | `world-model-kit` ← `../world-model-kit` | → `scene-kit` ← `../scene-kit` |
| 6 | `README.md` | L72 | `含 RECS / world-model-kit 本地可编辑依赖` | → `含 RECS / scene-kit 本地可编辑依赖` |
| 7 | `docs/开发日志.md` | L27 | 2026-07-31 历史条目提及 `world-model-kit` | **保留原名**，另新增一条迁移日志 |
| 8 | `CHANGELOG.md` | L10 | 历史条目提及 `../world-model-kit` | **保留原名**，新增条目标注改名 |

## 4. 明确不做修改的位置

- `EntelechySystem/` 源码目录：核查无 `world_model_kit` import，公共 API 不变，**无需改动**。
- `relation-entity-component-system/.idea/workspace.xml`：仅 IDE 最近打开记录引用旧路径，非代码引用，**不动**。
- `AgentsWorldSystem`：核查无旧名引用，**不动**。
- `ES_notebook`、`ES_theory`：核查无旧名引用，**不动**。
- 历史条目（开发日志 2026-07-31、CHANGELOG）按信件指示**保留原名**，仅追加新条目。

## 5. 执行方案

### 5.1 修改内容（8 处，2 个文件为主）

```bash
# pyproject.toml（2 处）
#   L12:  "world-model-kit>=0.2.0"  →  "scene-kit>=0.2.0"
#   L21:  world-model-kit = { path = "../world-model-kit", editable = true }
#         → scene-kit = { path = "../scene-kit", editable = true }

# scripts/uv_migrate.sh（2 处）
#   L12: 注释 RECS / world-model-kit → RECS / scene-kit
#   L119: check_import world_model_kit → check_import scene_kit

# docs/开发者指南.md（1 处，L26）
#   world-model-kit ← ../world-model-kit → scene-kit ← ../scene-kit

# README.md（1 处，L72）
#   RECS / world-model-kit → RECS / scene-kit
```

### 5.2 新增条目（2 处）

- `docs/开发日志.md`：新增一条 **2026-08-14** 迁移日志（类型：依赖；内容：world-model-kit → scene-kit 品牌迁移；影响：仅包元数据与本地路径，API 不变；验证：uv sync + 导入验证）。
- `CHANGELOG.md`：新增条目标注改名。

### 5.3 迁移命令（信件提供，可执行）

```bash
cd /Users/ethan/CoreFiles/ProjectsFile/EntelechySystem
sed -i '' 's/world-model-kit/scene-kit/g' pyproject.toml
uv sync
```

> 注：信件命令只覆盖 `pyproject.toml`；其余文件（脚本、文档）由人工逐处替换，避免误伤历史条目。

## 6. 验证步骤

1. `bash scripts/uv_migrate.sh` 或 `uv sync`：依赖解析成功。
2. `uv run python -c "import scene_kit"`：导入成功（uv_migrate.sh 的 `check_import` 已改为 `scene_kit`）。
3. `uv run python -c "import EntelechySystem"` 相关引擎导入验证（沿用现有脚本逻辑）。
4. `grep -rn "world-model-kit\|world_model_kit" --include="*.toml" --include="*.py" --include="*.md" .`：确认仅剩历史条目与 inbox 信件中的旧名。

## 7. 收尾动作（执行后建议）

- 确认 `docs/mailbox/relay/in-up/scene-kit.md` 信件已处理（可在信件内追加 `handled_at` 标记）。
- 如需回执，可在 `out-up/` 写信告知 scene-kit 迁移完成；或由投递事务智能体统一处理。
- 涉及其余下游（ComplexSystemLab、yeegames）的信件为同一迁移主题，如需一并推进可另行计划。

## 8. 待审阅决策点

1. 是否按上表执行全部 8 处修改？（建议：是）
2. 历史条目是否确认保留原名、仅追加新日志？（信件明确指示：是）
3. 是否需要同时更新 `relation-entity-component-system/.idea/workspace.xml` 的 IDE 路径记录？（建议：否，非代码）
4. 迁移完成后是否写回执信给 scene-kit？（可选）
