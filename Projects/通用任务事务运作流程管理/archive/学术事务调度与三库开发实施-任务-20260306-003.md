# 学术事务调度与三库开发实施-任务-20260306-003

## 1. 本轮目标

将项目发行名从 `academic-research-toolbox` 调整为 `academic-research-kit`，并验证重新安装后的发行元数据是否生效。

## 2. 可行性分析

1. 当前导入名已经稳定为 `academicresearch_kit`，发行名调整不会影响既有 Python 导入路径。
2. 发行名主要影响包安装元数据和构建产物，核心修改点应集中在 `pyproject.toml`。
3. `.egg-info` 属于构建产物，重新执行可编辑安装即可刷新，无需手工维护。

## 3. 实施步骤

1. 修改 `pyproject.toml` 中的 `[project].name`。
2. 保持现有 CLI 与导入名不变。
3. 重新执行 `uv pip install -e .` 验证发行名更新。
4. 记录结果。

## 4. 实施结果

1. 已将 `pyproject.toml` 中的发行名修改为 `academic-research-kit`。
2. 已将主脚本入口从 `academic-research-toolbox` 同步调整为 `academic-research-kit`。
3. 重新执行可编辑安装后，安装输出显示：`academic-research-kit==0.2.1`。
4. 通过 `importlib.metadata` 校验，`Name` 字段返回 `academic-research-kit`。
5. 通过命令行校验，`academic-research-kit --help` 可正常输出帮助信息。

## 5. 结论

1. 当前项目的发行名已经切换到 `academic-research-kit`。
2. 导入名仍保持为 `academicresearch_kit`，不受发行名调整影响。
3. 现有 `ark-*` CLI 不受影响，可继续供 Agents/Skills 使用。

## 6. 追加实施进度

1. 已将 `academicresearch_kit` 包内所有原 `src.*` 导入切换为正式包路径，完成逻辑迁移收口。
2. 已同步更新 `README.md`、`docs/用户手册.md`、`docs/开发者指南.md` 与相关演示命令示例中的发行名/CLI 示例，统一为 `academic-research-kit`。
3. 当前仍保留部分 `academic_research_toolbox` 路径说明，用于指向仓库内现存旧代码目录，不属于发行名残留。
4. `src/` 目录已从仓库中移除，核心实现现在仅保留在 `academicresearch_kit/` 下维护。
5. 已完成回归验证：
	- `import academicresearch_kit as ark` 成功，`ark.SchemaRegistry.default().version` 返回 `0.1.0`。
	- `ark.run` 已通过顶层包兼容导出对齐旧调度器调用方式。
	- `ark-taskdb-validate` 返回 `error_count = 0`。
	- `ark-taskdb-replay` 返回空对象 `{}`。
	- `ark-retrieval` 与 `ark-scheduler --execute` 均可正常输出结构化结果。
	- `academic-research-kit --help` 可正常输出帮助信息。
