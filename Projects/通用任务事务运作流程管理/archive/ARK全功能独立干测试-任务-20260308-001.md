# ARK全功能独立干测试-任务-20260308-001

## 需求与目标

- 对 ARK 每个功能做一次单独干测试。
- 测试必须不依赖 opencode。
- 使用 `references/origin/user_uploads` 作为样本输入。
- 按需使用 `.github/config/bailian-api-key.txt` 验证百炼模型调用。
- 最终在 `.github/docs/report` 产出完整报告表。

## 可行性分析

- ARK 已存在可脚本化 skill、CLI、API 门面与主入口函数，具备独立调用条件。
- 样本数据目录已提供，可覆盖本地文献导入、预筛选、阅读计划链路。
- 百炼 key 文件已就位，可对模型路由进行一次实连校验。
- 仍有部分能力为文档型 skill，当前没有可执行代码入口，应在报告中标记为 `BLOCKED`。

## 计划步骤

1. 盘点 ARK 全功能清单并建立测试矩阵。
2. 编写并运行全功能独立干测试脚本。
3. 修复测试脚本参数问题并复跑，得到最终结果。
4. 生成 `.github/docs/report` 正式报告。

## 实施记录

- 新增脚本：`academicresearch_kit/demos/run_ark_full_feature_drytests.py`。
- 该脚本覆盖：`core`、`skill`、`api`、`cli`、`unscripted-skill` 五类能力。
- 首轮运行发现 `ark-scheduler` 参数不匹配，已修复后复跑。
- 复跑后统计：`PASS=36`、`ERROR=0`、`BLOCKED=1`、`TOTAL=37`。

## 结果与进度总结

- 可执行功能已全部通过，未出现运行时失败。
- 阻塞项均为未脚本化能力，属于实现缺口而非测试失败。
- 正式报告：`C:\Users\Ethan\CoreFiles\ProjectsFile\AcademicResearch-auto-workflow\.github\docs\report\report-ARK全功能独立干测试-20260308-001.md`。
- 结构化结果：`C:\Users\Ethan\CoreFiles\ProjectsFile\AcademicResearch-auto-workflow\demos\ark_full_feature_drytest_20260308_001\ark_full_feature_test_summary.json`。

## 下一步建议

1. 优先将 `BLOCKED` 的 14 个文档型 skill 分批脚本化，每批 2-3 个。
2. 给 `run_ark_full_feature_drytests.py` 增加 CI 入口，形成每次提交自动回归。
3. 补充更多 workflow 样本，增加 `dry_run=False` 的事务级真实执行覆盖面。
