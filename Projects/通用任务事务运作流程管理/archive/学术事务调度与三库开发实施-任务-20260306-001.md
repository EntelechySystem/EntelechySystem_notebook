# 学术事务调度与三库开发实施-任务-20260306-001

## 1. 任务背景

### 1.1 输入

1. 已有总计划文档：` .github/docs/plan/plan-学术事务调度与三库开发实施总计划-20260306-001.md`
2. 用户新增约束：当前阶段先直接使用纯 Python 开发，不使用 AOK。
3. 用户新增执行要求：先创建 `Python 3.13 + venv + uv pip` 环境，然后继续开发；本轮先不调试。

### 1.2 可行性分析

1. 当前仓库 `src/` 目录为空，适合直接承载新的原型实现，不会与既有业务入口发生强耦合。
2. `database/tasks`、`database/references`、`database/knowledge` 当前为空，可直接写入模板文件与 manifest。
3. 现阶段不接入 AOK，改为纯 Python 派发执行器是可行的；后续只需把 `dispatch_map` 从 Python handler 替换到 AOK workflow 映射即可。
4. 本轮不调试意味着可以先完成结构化实现、接口契约和模板文件；验证工作留在下一轮执行。

## 2. 实施计划

1. 创建并修复项目虚拟环境，统一为 `Python 3.13`。
2. 在 `src/scheduler_core/` 实现纯 Python 调度闭环。
3. 在 `src/tasking_db/` 实现 CSV/JSONL 任务数据库管理模块。
4. 在 `src/retrieval_governance/` 实现统一检索状态机与 bundle 输出。
5. 在 `config/scheduler/` 写入调度策略模板。
6. 在三类数据库目录下写入初始模板文件。
7. 记录本轮实施结果与后续建议。

## 3. 实施过程

### 3.1 环境实施

1. 检查到仓库原有 `.venv` 残缺，仅存在 `Scripts/` 目录。
2. 将残缺环境重命名备份为 `.venv.bak-20260306-001`。
3. 使用 `py -3.13 -m venv .venv` 重建虚拟环境。
4. 使用环境内 `pip` 升级 `pip`，并通过 `uv pip install -e .` 安装当前项目。

### 3.2 调度内核实施

已新增以下文件：

1. `src/__init__.py`
2. `src/scheduler_core/__init__.py`
3. `src/scheduler_core/types.py`
4. `src/scheduler_core/candidate_builder.py`
5. `src/scheduler_core/edge_scorer.py`
6. `src/scheduler_core/route_selector.py`
7. `src/scheduler_core/route_guard.py`
8. `src/scheduler_core/dispatch_executor.py`
9. `src/scheduler_core/event_logger.py`
10. `src/scheduler_core/service.py`

实现内容：

1. 建立候选边、调度上下文、评分结果、守卫决策、派发回执和调度事件等结构化类型。
2. 实现候选构建、评分、路由选择、结果码守卫、纯 Python 派发和 JSONL 日志输出。
3. 派发器明确绕开 AOK，改为支持 `python_callable`、`python_module` 与 `placeholder` 三类目标。

### 3.3 任务数据库实施

已新增以下文件：

1. `src/tasking_db/__init__.py`
2. `src/tasking_db/schema_registry.py`
3. `src/tasking_db/csv_store.py`
4. `src/tasking_db/state_machine.py`
5. `src/tasking_db/integrity_checks.py`
6. `src/tasking_db/snapshot_manager.py`
7. `src/tasking_db/replay_engine.py`
8. `src/tasking_db/bootstrap.py`

实现内容：

1. 定义任务主表、事务表、映射表和边表的 Schema 注册表。
2. 实现 CSV 原子写入、JSON/JSONL 产物存储。
3. 实现任务状态机、事务状态机、完整性校验、快照恢复和执行日志回放。
4. 实现项目级模板引导器，为后续自动初始化保留统一入口。

### 3.4 检索治理实施

已新增以下文件：

1. `src/retrieval_governance/__init__.py`
2. `src/retrieval_governance/engine.py`

实现内容：

1. 定义检索请求和检索 bundle 数据模型。
2. 实现四维标注后的统一授权判定逻辑。
3. 对闭源链路返回 `manual_required` 并映射为 `BLOCKED`。
4. 实现 bundle 路由器，为后续接入文献库和知识库留出接口。

### 3.5 配置与数据库模板实施

已写入以下模板：

1. `config/scheduler/edge_weights.json`
2. `config/scheduler/selection_policy.json`
3. `config/scheduler/guard_policy.json`
4. `config/scheduler/dispatch_map.json`
5. `database/tasks/*`
6. `database/references/*`
7. `database/knowledge/*`

说明：

1. 当前模板以最小可运行字段为主，符合原型优先原则。
2. `dispatch_map.json` 中已把检索派发映射到纯 Python handler。
3. 向量索引和 notes 目录使用 `.gitkeep` 占位，便于后续直接落文件。

## 4. 当前结果

### 4.1 已完成

1. 项目开发环境已按 `Python 3.13 + venv + uv pip` 建立完成。
2. 纯 Python 调度内核已完成第一版代码落地。
3. 任务数据库管理系统已完成第一版代码落地。
4. 检索治理状态机已完成第一版代码落地。
5. 三类数据库模板和调度配置模板已落盘。
6. 本轮任务文档已创建并记录实施结果。

### 4.2 本轮未做

1. 未运行测试。
2. 未执行调度样例。
3. 未进行语法调试、集成调试和数据联调。
4. 未接入现有主入口 `academic_research_toolbox/main.py`。

## 5. 下一步建议

1. 增加一个单独的 CLI 入口，把 `SchedulerService`、`ProjectBootstrapper` 和 `RetrievalGovernanceEngine` 串成最小运行命令。
2. 按计划补一版种子事务定义与样例边表，形成至少 1 条“研究问题 -> 文献检索 -> 文献管理”的可执行链路。
3. 在下一轮开始静态校验与最小联调，优先检查导入路径、类型拼装和 JSONL/CSV 落盘行为。
