# 学术事务调度与三库开发实施-任务-20260307-006

## 1. 本轮目标

1. 补一个最小可运行的 `workflow.json`，并把 `config/config.json` 指向它。
2. 基于正式包入口 `academicresearch_kit` 执行一次真正的端到端 dry-run。
3. 单独清理 `docs/` 与 `academicresearch_kit/docs/` 中残留的旧目录说明，使文档统一收口到 `academicresearch_kit`。

## 2. 可行性分析

1. 当前主入口 `academicresearch_kit.main` 已具备读取 `config.json` 与摘要 `workflow.json` 的能力，适合补一个最小工作流进行 dry-run 验证。
2. 当前 `config/config.json` 已存在，只需补充 `run` 列表即可完成本轮最小链路接通。
3. 文档层面的旧目录说明主要集中在用户手册与开发者指南，适合本轮同步收口。

## 3. 实施计划

1. 新建最小 dry-run workflow 示例与说明文档。
2. 更新 `config/config.json` 指向该 workflow。
3. 执行 `academicresearch_kit` 主入口和控制台脚本的端到端 dry-run。
4. 清理 `docs/` 与 `academicresearch_kit/docs/` 中的旧目录说明。
5. 回填验证结果与后续建议。

## 4. 实施进度

1. 已开始补齐本轮任务文档。
2. 已新建最小 workflow 示例：`demos/workflows/workflow_最小dry_run/workflow.json`，并补充对应 `README.md`。
3. 已更新 `config/config.json`，将默认 `run` 列表指向 `demos/workflows/workflow_最小dry_run/workflow.json`。
4. 已更新 `docs/用户手册.md` 与 `academicresearch_kit/docs/用户手册.md`，补充最小 dry-run workflow 的使用说明，并清理旧目录表述。
5. 已重写 `academicresearch_kit/docs/开发者指南.md`，将文档整体收口到当前有效的 `academicresearch_kit` 包结构、入口和配置约定。

## 5. 验证结果

1. 在 `.venv` 中执行 `python -m pip install -e .` 成功完成重新安装。
2. 编程入口验证成功：

```python
import academicresearch_kit as ark
summary = ark.run(config_path="config/config.json", dry_run=True)
```

返回结果关键字段如下：

```json
{
	"workflow_count": 1,
	"missing_workflow_count": 0,
	"workflows": [
		{
			"workflow_id": "workflow_最小dry_run",
			"affair_count": 1,
			"flow_count": 1,
			"affair_keys": ["dry_run_probe"],
			"flow": ["dry_run_probe"]
		}
	]
}
```

3. 模块入口验证成功：`python -m academicresearch_kit.main --config config/config.json --dry-run` 输出与编程入口一致。
4. 控制台脚本验证成功：`academic-research-kit --config config/config.json --dry-run` 输出与模块入口一致。

## 6. 结果总结

1. 当前仓库已经具备一个真实存在、可被 `config/config.json` 读取到的最小 workflow，能够完成端到端 dry-run。
2. 文档层面已将用户可见的旧目录说明收口到 `academicresearch_kit`，开发者指南也已切换为当前真实结构。
3. 本轮没有扩展真实事务执行逻辑，只完成了配置链路打通与文档清理，符合“先做 dry-run 验证”的目标。

## 7. 下一步建议

1. 若要继续推进执行能力，可在 `academicresearch_kit/main.py` 的 dry-run 摘要基础上增加真实 workflow 执行阶段。
2. 若要补更多示例，可继续在 `demos/workflows/` 下新增主题化 workflow，并按需切换 `config/config.json.run`。
