# ARK技能AOK事务化-任务-20260311-001

## 任务背景

- 需求：将 ARK 剩余候选 skill 继续收口到 AOK affairs，保持 `academicresearch_kit/skills -> academicresearch_kit/api -> autodokit.affairs` 的统一调用链。
- 本轮目标：优先落地 `workspace_sanity_check` 与 `project_bootstrap`，避免继续保留 ARK 侧重复实现。

## 可行性分析

- `workspace_sanity_check` 当前逻辑完整且职责单一，适合一对一迁移为 AOK `工作区自检` affair。
- `project_bootstrap` 本质由“可选自检 + 项目骨架初始化”两段组成，其中初始化已经可直接复用 AOK `ProjectBootstrapper`，因此适合拆为“自检事务 + 初始化事务”，由 ARK skill 侧做轻量编排。
- 该改造不会改变上层 skill 的主要输入输出语义，兼容现有 smoke 测试扩展方式，风险可控。

## 计划步骤

1. 检查当前任务文档与已有迁移基线。
2. 在 AOK 新增 `工作区自检` affair。
3. 在 AOK 新增 `项目初始化` affair。
4. 在 ARK 新增对应 API 门面，并将两个 skill 改为只调用 AOK affair。
5. 补充 smoke 回归测试并执行验证。
6. 回写本任务文档的实施进度、结果与下一步建议。

## 实施进度

- 已完成：确认当前任务文档不存在，已建立本轮任务文档。
- 已完成：在 AOK 新增 `工作区自检` affair，复用并收口原 ARK `workspace_sanity_check` 的核心逻辑。
- 已完成：在 AOK 新增 `项目初始化` affair，基于 AOK 现有 `CsvStore`、`JsonArtifactStore`、`SchemaRegistry` 直接封装项目骨架初始化能力。
- 已完成：在 ARK 新增 `workspace_sanity_check`、`project_bootstrap` 两个 API 门面，并将对应 skill 改为只调用 AOK affair。
- 已完成：为两个 skill 新增 smoke 用例；当前 `tests/test_migration_smoke.py` 共 16 条用例，已全部通过。
- 已完成：为避免无关旧桥接契约阻断本轮验证，补充修复了以下最小兼容层：
	- AOK `autodokit.scheduling.__init__` 恢复顶层导出，满足 ARK 调度门面既有符号契约。
	- ARK `academicresearch_kit`、`academicresearch_kit.api`、`academicresearch_kit.skills` 三层包入口改为懒加载，避免导入单个 skill 时被无关 scheduler/taskdb 门面提前拉起。

## 实施结果

- 新增 AOK affairs：`工作区自检`、`项目初始化`。
- 新增 ARK API 门面：`academicresearch_kit/api/workspace_sanity_check.py`、`academicresearch_kit/api/project_bootstrap.py`。
- 改造 ARK skills：`academicresearch_kit/skills/workspace_sanity_check.py`、`academicresearch_kit/skills/project_bootstrap.py`。
- 回归验证：`C:/Users/Ethan/CoreFiles/ProjectsFile/AcademicResearch-auto-workflow/.venv/Scripts/python.exe -m pytest tests/test_migration_smoke.py -q`，结果为 `16 passed in 7.01s`。

## 下一步建议

- 完成本轮后，按既定分析顺序继续推进 `model_routing_dispatch`、`aliyun_bailian_sdk_bootstrap`、`cn_local_library_manager`、`cn_web_acquisition`、`whitelist_governance_check` 的事务化收口。