# ARK全功能独立干测试-任务-20260308-002

## 需求与目标

- 在上一轮独立干测试基础上，继续推进 `BLOCKED=14` 的未脚本化能力落地。
- 形成可直接执行的分批实施清单，作为下一阶段开发入口。
- 保持不依赖 opencode 的独立运行口径。

## 可行性分析

- 已有测试报告明确了 14 项阻塞能力均为“文档型 skill，缺少代码入口”，问题边界清晰。
- 现有 ARK 代码结构（`academicresearch_kit/skills`、`academicresearch_kit/cli`、`academicresearch_kit/api`）已具备扩展位点。
- 可采用“先最小可运行实现（MVP），再补参数与外部依赖”的策略，降低一次性改造风险。
- 风险点主要在外部依赖（订阅库、网页采集、R/Stata 环境）与方法实现复杂度（计量/审稿流程），可通过分批上线控制。

## 计划步骤

1. 将 14 项阻塞能力按依赖复杂度分 4 批落地。
2. 每批统一实现三层入口：`skills` 函数、对应 CLI、最小 API 门面导出。
3. 每批完成后复用现有全功能干测试脚本进行回归。
4. 批次完成即更新报告与任务文档，保证可追溯。

## 实施记录（本轮）

本轮以“文档实施”为主，完成阻塞项落地方案设计与优先级编排。

### 分批实施清单

- 批次 A（低依赖，优先）
  - `integrity-check`
  - `method-whitelist-selection`
  - `variable-operationalization`
  - `paper-draft`
  - 目标：先提供本地规则与文本处理能力，不依赖外部服务。

- 批次 B（本地数据流程）
  - `cn-local-library-manager`
  - `rag-evidence-synthesis`
  - `revision-response`
  - 目标：围绕 `references/origin/user_uploads` 建立可执行流水线。

- 批次 C（外部接入）
  - `aliyun-bailian-sdk-bootstrap`
  - `cn-web-acquisition`
  - `cn-subscribed-literature-access`
  - 目标：补充密钥读取、联网抓取与授权访问占位实现。

- 批次 D（高复杂度方法）
  - `did-rdd-pipeline`
  - `econometrics-runtime-setup`
  - `empirical-four-pack`
  - `whitelist-governance-check`
  - 目标：建立可运行骨架与参数校验，再逐步补齐完整方法。

### 每项能力最小交付标准（DoD）

- 在 `academicresearch_kit/skills` 中具备可调用 Python 入口。
- 至少一个对应 CLI 可运行（`--help` + 最小 dry-run）。
- 全功能干测试脚本可新增并通过该项用例。
- 错误信息可诊断（参数缺失、路径缺失、外部服务不可达）。

## 结果与进度总结

- 已完成：阻塞项全量拆解、优先级排序、批次化推进方案。
- 当前状态：可直接进入“批次 A”编码实施。
- 预期收益：优先将 `BLOCKED` 从 14 降到 10（完成批次 A 后），并维持 `ERROR=0`。

## 下一步建议

1. 立即执行批次 A（4 项），先打通最小可运行入口。
2. 同步在 `run_ark_full_feature_drytests.py` 中加入这 4 项用例并复跑。
3. 批次 A 完成后生成 `task-ARK全功能独立干测试-20260308-003.md` 记录实装结果。