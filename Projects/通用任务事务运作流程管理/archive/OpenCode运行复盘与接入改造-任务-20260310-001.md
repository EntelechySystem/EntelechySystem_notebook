# OpenCode运行复盘与接入改造-任务-20260310-001

## 1. 需求与目标

根据用户需求，本轮任务目标如下：

1. 解释 `/run-fusion-p4-e2e` 与相关子命令职责。
2. 复盘刚完成的 OpenCode 运行状态，并按时间顺序出具运行报告。
3. 给出模型使用证据（是否真实调用阿里百炼）。
4. 实现一键接入阿里百炼 API Key 的工程化入口。
5. 实现每任务/事务的统一流程轨迹日志（包含 agent、skill、model、task、transaction）。
6. 同步更新相关手册与 API 文档，减少文档过时问题。

## 2. 可行性分析

### 2.1 当前基础能力

1. 仓库已有模型路由技能与百炼兼容调用实现。
2. 仓库已有 P4 试运行产物和审计文档，可用于复盘。
3. 仓库已有日志目录结构，可直接扩展统一轨迹日志。

### 2.2 关键差距

1. 缺少“一键接入”统一命令。
2. 缺少跨任务/事务统一 JSONL 轨迹。
3. 文档存在脚本路径与实现不一致（旧脚本名）。

### 2.3 实施策略

1. 新增 CLI 入口实现一键接入。
2. 在关键技能与调度 CLI 注入统一轨迹日志。
3. 修复命令与技能文档中的旧脚本路径并补充验收判据。
4. 输出独立运行复盘报告到 `.github/docs/report`。

## 3. 计划步骤

1. 盘点本次 OpenCode 运行产物和日志证据。
2. 建立按时间顺序的运行复盘基线。
3. 实现一键百炼接入 CLI。
4. 实现统一流程轨迹日志工具并接入关键执行点。
5. 同步更新规则、手册、API 文档。
6. 生成正式复盘报告与本任务文档。

## 4. 实施记录

### 4.1 运行复盘与证据盘点

1. 读取 `logs/run/model-routing-decision-log.md`，确认本次为 `decision-only`。
2. 读取 `logs/run/2026-03-10_opencode-pipeline-supervision.md`，确认 19:15 与 19:25 两个关键节点。
3. 读取审计与改进文件，确认 P4 试运行完成但存在方法与数据局限。

### 4.2 一键接入能力实现

新增文件：

- `academicresearch_kit/cli/opencode_bailian_bootstrap.py`

新增命令：

- `ark-opencode-bailian-bootstrap`

实现内容：

1. 校验 `.github/config/bailian-api-key.txt`。
2. 执行百炼接入检查技能。
3. 执行模型路由验证（支持 `--execute` 实连）。
4. 写入运行时配置 `.opencode/runtime/bailian-runtime.json`。

### 4.3 统一轨迹日志实现

新增文件：

- `academicresearch_kit/tools/flow_trace.py`

接入点：

1. `academicresearch_kit/skills/model_routing_dispatch.py`
2. `academicresearch_kit/skills/aliyun_bailian_sdk_bootstrap.py`
3. `academicresearch_kit/cli/scheduler.py`

输出文件：

- `logs/run/opencode-flow-trace-YYYY-MM-DD.jsonl`

记录字段：

- `agent`
- `skill`
- `provider`
- `model`
- `task_uid`
- `transaction_uid`
- `status`
- `mode`
- `artifacts`

### 4.4 文档与规则同步

更新文件：

1. `.opencode/commands/run-model-routing.md`
2. `.opencode/skills/model-routing-dispatch/SKILL.md`
3. `.opencode/skills/aliyun-bailian-sdk-bootstrap/SKILL.md`
4. `.opencode/rules/prefer-venv-python.md`
5. `.opencode/rules/mandatory-step-logging-supervision.md`
6. `.github/docs/manual/manual-学术科研工作流初始模板结构与操作指南-20260310-001.md`
7. `academicresearch_kit/docs/API手册.md`

## 5. 产出清单

1. 运行复盘报告：
   - `.github/docs/report/report-OpenCode运行复盘与接入改造-20260310-002.md`
2. 一键接入 CLI：
   - `academicresearch_kit/cli/opencode_bailian_bootstrap.py`
3. 统一轨迹日志工具：
   - `academicresearch_kit/tools/flow_trace.py`
4. 配置入口注册：
   - `pyproject.toml`

## 6. 结果总结

1. 已可通过单条命令完成百炼接入校验与模型路由验证。
2. 已具备任务/事务级结构化轨迹日志基础能力。
3. 已修复关键文档与实现路径不一致问题。
4. 已形成本次 OpenCode 运行的时间序复盘报告。

## 7. 下一步建议

1. 把轨迹日志继续接入 `run-scholar-research-init`、`run-rag-econometrics-pipeline`、`run-scholar-analyze-results` 的实际执行代码，形成全链路覆盖。
2. 基于 `.github/docs/manual/opencode-学术研究流程架构-20260303.md` 重写 `docs/用户手册.md`，统一“命令协议/代理协同/日志验收”结构。
3. 将轨迹日志与 `database/tasks/execution_runs.jsonl` 自动关联，形成可回放任务账本。
