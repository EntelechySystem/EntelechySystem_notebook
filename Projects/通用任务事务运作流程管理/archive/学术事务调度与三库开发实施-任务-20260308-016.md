# 学术事务调度与三库开发实施-任务-20260308-016

## 1. 需求与目标

### 1.1 需求

继续执行结构统一重构阶段 C：将 `tasking_db` 从旧目录迁移到 `academicresearch_kit/platform/taskdb`，并在迁移完成后统一更新文档。

### 1.2 目标

1. 新建 `academicresearch_kit/platform/taskdb/` 作为 taskdb 正式实现目录。
2. 将 `academicresearch_kit/api/taskdb.py` 切换到新正式路径。
3. 将 `academicresearch_kit/tasking_db/*` 调整为兼容转发层。
4. 同步更新 `academicresearch_kit/docs/开发者指南.md` 与 `academicresearch_kit/docs/API手册.md`。
5. 对迁移后的关键文件执行错误检查。

## 2. 可行性分析

### 2.1 现状匹配

1. `tasking_db` 已具备清晰的模块边界：bootstrap、csv_store、schema_registry、integrity_checks、state_machine、snapshot_manager、replay_engine。
2. 阶段 A 已建立 `platform/` 目录，具备承接数据平面正式实现的位置。
3. 阶段 B 已完成“正式实现迁移 + 兼容层保留”的范式，可直接复用于阶段 C。

### 2.2 风险与控制

1. 风险：taskdb 模块内部互相导入，迁移时容易漏改路径。
2. 风险：文档可能仍把 `tasking_db/` 误写为正式实现位置。
3. 控制：先完整复制到 `platform/taskdb`，再把旧目录逐个降为兼容层，最后统一做错误检查与文档同步。

结论：阶段 C 可按最小风险方式直接实施。

## 3. 实施过程

### 3.1 正式实现迁移

已新增：

1. `academicresearch_kit/platform/taskdb/__init__.py`
2. `academicresearch_kit/platform/taskdb/bootstrap.py`
3. `academicresearch_kit/platform/taskdb/csv_store.py`
4. `academicresearch_kit/platform/taskdb/integrity_checks.py`
5. `academicresearch_kit/platform/taskdb/replay_engine.py`
6. `academicresearch_kit/platform/taskdb/schema_registry.py`
7. `academicresearch_kit/platform/taskdb/snapshot_manager.py`
8. `academicresearch_kit/platform/taskdb/state_machine.py`

### 3.2 正式入口切换

已更新：

1. `academicresearch_kit/api/taskdb.py`：正式导入源改为 `academicresearch_kit.platform.taskdb.*`
2. `academicresearch_kit/platform/__init__.py`：补充 taskdb 平台导出

### 3.3 兼容层保留

已将以下旧路径文件改为兼容转发层：

1. `academicresearch_kit/tasking_db/__init__.py`
2. `academicresearch_kit/tasking_db/bootstrap.py`
3. `academicresearch_kit/tasking_db/csv_store.py`
4. `academicresearch_kit/tasking_db/integrity_checks.py`
5. `academicresearch_kit/tasking_db/replay_engine.py`
6. `academicresearch_kit/tasking_db/schema_registry.py`
7. `academicresearch_kit/tasking_db/snapshot_manager.py`
8. `academicresearch_kit/tasking_db/state_machine.py`

## 4. 文档同步

本轮已同步更新：

1. `academicresearch_kit/docs/开发者指南.md`
2. `academicresearch_kit/docs/API手册.md`

更新重点：

1. taskdb 正式实现位置改为 `academicresearch_kit/platform/taskdb/`
2. `academicresearch_kit/tasking_db/` 明确标记为兼容层
3. 公开 API 继续保持 `academicresearch_kit.api.taskdb.*` 不变

## 5. 验证结果

已检查通过：

1. `academicresearch_kit/api/taskdb.py`
2. `academicresearch_kit/platform/__init__.py`
3. `academicresearch_kit/platform/taskdb/`
4. `academicresearch_kit/tasking_db/`

一致性检查结果：

1. `academicresearch_kit.api.taskdb.*` 已统一从 `academicresearch_kit.platform.taskdb.*` 导入正式实现。
2. `academicresearch_kit.tasking_db/*` 仅保留兼容转发，不再承载正式实现。
3. `academicresearch_kit/docs/开发者指南.md` 与 `academicresearch_kit/docs/API手册.md` 已同步反映当前结构。

当前未发现迁移后显式错误，阶段 C 已完成代码与文档双收口。

## 6. 当前进度结论

1. 阶段 C 主目标已完成：taskdb 正式实现已迁入 `academicresearch_kit/platform/taskdb/`。
2. 旧路径 `academicresearch_kit/tasking_db/` 仍可继续使用，但已退化为兼容层。
3. 到本阶段结束，`api / application / domains / platform / compat` 的整体分层已基本成形。

## 7. 下一步建议

1. 进入阶段 D：将 `main.py`、`workflow_executor.py`、`workflow_runtime.py` 等应用编排入口迁入 `application/`。
2. 在阶段 D 完成后，再进行一次总体验收与兼容层清理评估。
