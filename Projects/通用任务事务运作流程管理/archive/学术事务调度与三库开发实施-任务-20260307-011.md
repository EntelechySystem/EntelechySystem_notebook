# 学术事务调度与三库开发实施-任务-20260307-011

## 1. 本轮目标

1. 在 `model-routing-dispatch` 与 `rag-evidence-synthesis` 中选择一个更适合当前仓库现状的 skill，开始脚本化。
2. 将可复用的模型路由逻辑优先沉到 `academicresearch_kit/tools/` 与 `academicresearch_kit/skills/`。
3. 为 `.claude/skills/model-routing-dispatch/` 提供最小可执行包装脚本。
4. 同步更新 skill 接入清单、说明文档与本轮结果。

## 2. 可行性分析

1. `model-routing-dispatch` 的输入、路由矩阵、回退规则与脚本路径约束已经在 `SKILL.md` 中写明，可直接转为首版代码实现。
2. 当前项目依赖已包含 `dashscope`，并且仓库已有调度、路由、治理等抽象，适合优先承接“模型选择决策”这一类独立工具能力。
3. 相比之下，`rag-evidence-synthesis` 目前只有高层目标描述，缺少结构化抽取字段、输入输出 schema 与现成仓库内代码支点，首轮脚本化成本更高。
4. 因此本轮优先选择 `model-routing-dispatch` 更稳妥，可先完成可解释路由与可选兼容接口调用，再为后续百炼/路由扩展铺路。

## 3. 实施计划

1. 创建模型路由工具模块，编码模型矩阵、风险/预算/时延规则与理由输出。
2. 创建 `academicresearch_kit.skills.model_routing_dispatch` 包内 skill 入口。
3. 新增 `.claude/skills/model-routing-dispatch/scripts/bailian_model_router.py` 包装脚本。
4. 更新 `academicresearch_kit/__init__.py`、`academicresearch_kit/skills/__init__.py` 与相关说明文档。
5. 执行最小 dry-run 验证并回填结果。

## 4. 实施进度

1. 已创建本轮任务文档。
2. 已完成候选评估，确定本轮优先脚本化 `model-routing-dispatch`。
3. 已创建 `academicresearch_kit/tools/model_routing.py`，完成任务类型矩阵、质量升降档、预算/时延/风险修正、模型名归一化、百炼兼容接口调用与产物落盘逻辑。
4. 已创建 `academicresearch_kit/skills/model_routing_dispatch.py`，提供包内 skill 入口 `run_model_routing_dispatch_skill(...)`。
5. 已创建 `.claude/skills/model-routing-dispatch/scripts/bailian_model_router.py`，支持 decision-only 模式与可选 `--execute` 实际调用。
6. 已更新 `academicresearch_kit/tools/__init__.py`、`academicresearch_kit/skills/__init__.py`、`academicresearch_kit/__init__.py` 以及 `academicresearch_kit/skills/README.md`、`academicresearch_kit/skills/未接入技能清单.md`、`academicresearch_kit/skills/未脚本化技能说明.md`、`.claude/skills/model-routing-dispatch/SKILL.md`。
7. 已完成最小 dry-run 验证：使用项目 `.venv` 运行包装脚本后返回 `status: PASS`、`mode: decision-only`、`decision.primary_model: qwen3.5-plus`、`fallback_models: ["glm-4.7"]`，并成功写出 `sandbox/model_router_demo/review/plan/model-routing-plan.md`、`sandbox/model_router_demo/logs/run/model-routing-decision-log.md`、`sandbox/model_router_demo/logs/audit/model-routing-cost-quality-audit.md`。

## 5. 验收口径

1. 存在可直接调用的包内 skill 入口。
2. 存在可直接运行的 `.claude` 包装脚本。
3. 路由结果包含主模型、备选模型和可追溯理由。
4. 未接入清单和说明文档与最新状态保持一致。

## 6. 本轮结果与建议

1. 本轮验收已通过，`model-routing-dispatch` 已从文档型 skill 进入可执行首版。
2. 当前默认采用 decision-only 路线，适合在无真实密钥或无需联网时先验证策略与文档产物链路。
3. 下一步若继续扩展，可优先做两件事：一是补真实 `--execute` 联调；二是以相同模式推进 `rag-evidence-synthesis` 的抽取与矩阵构建器。
