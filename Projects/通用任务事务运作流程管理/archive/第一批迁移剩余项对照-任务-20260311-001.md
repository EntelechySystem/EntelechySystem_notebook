# 第一批迁移剩余项对照-任务-20260311-001

## 1. 任务目标与对照范围

- 对照基准：`.github/docs/plan/plan-ARK技术层迁移AOK实施蓝图V1-20260310-001.md`
- 对照对象：`AcademicResearch-auto-workflow`（ARK）与 `autodo-kit`（AOK）
- 本轮目标：按蓝图收口剩余项，完成 S3/S4 的代码迁移与删除动作，并给出 S5 的下一步实施建议。

## 2. 可行性分析

1. S3 可行性：已存在 AOK 调度内核与 ARK API 门面，主要风险在“残留导入 + 旧目录删除后的引用断裂”。
2. S4 可行性：ARK `platform/taskdb` 结构清晰，可整体迁入 AOK 新包并以 ARK API 门面代理回收调用面。
3. S5 风险：涉及 `domains/cnki`、`domains/retrieval_governance`、`domains/local_reference_ingestion`、`domains/literature_reading`、`domains/knowledge_prescreen` 与多 Skill 输出契约联动，需分批迁移与回归验证，不能在未做契约映射前直接删域实现。

## 3. 实施计划

1. 完成 S3 收口：删除 ARK 旧调度实现目录，清理执行器遗留。
2. 完成 S4 主体迁移：在 AOK 建立 `autodokit/taskdb/*`，并把 ARK taskdb 调用面切到 AOK。
3. 清理蓝图删除清单：删除 ARK `workflow_executor.py`、`workflow_runtime.py`、`platform/taskdb/`、`domains/scheduling/`。
4. 同步文档：更新 API 手册中的实现位置与入口行为说明。
5. 验证：全局引用检索 + `get_errors` 编译/静态检查。

## 4. 本轮实施记录

### 4.1 S3 收口实施

1. 已删除旧调度目录：`academicresearch_kit/domains/scheduling/`。
2. 已删除遗留执行器文件：`academicresearch_kit/workflow_executor.py`。
3. AOK 运行时调度接入保持生效（此前已接入）：`autodokit/flow_graph/node_runtime_v2.py`。

### 4.2 S4 主体迁移实施

1. 已在 AOK 新增 taskdb 包：
- `autodokit/taskdb/__init__.py`
- `autodokit/taskdb/bootstrap.py`
- `autodokit/taskdb/csv_store.py`
- `autodokit/taskdb/schema_registry.py`
- `autodokit/taskdb/state_machine.py`
- `autodokit/taskdb/replay_engine.py`
- `autodokit/taskdb/integrity_checks.py`
- `autodokit/taskdb/snapshot_manager.py`
2. ARK taskdb 门面已切到 AOK：`academicresearch_kit/api/taskdb.py`。
3. ARK 平台导出已切门面：`academicresearch_kit/platform/__init__.py` 改为经 `api.taskdb` 导出。
4. 已删除旧 taskdb 目录：`academicresearch_kit/platform/taskdb/`。

### 4.3 ARK 主入口与删除清单收口

1. `academicresearch_kit/main.py` 已改为委托 AOK 入口执行。
2. 已删除 `academicresearch_kit/workflow_runtime.py`。
3. 旧删除清单中的 S3/S4 相关项已落地完成。

### 4.4 文档同步

1. 已更新 `academicresearch_kit/docs/API手册.md`：
- 顶层 `run(...)` 行为更新为“委托 `autodokit.main.run`”。
- 新增 `engine` 返回字段说明。
- taskdb 实现位置由 `academicresearch_kit.platform.taskdb.*` 更新为 `autodokit.taskdb.*`。

## 5. 验证结果

1. 代码引用检索：`academicresearch_kit/**/*.py` 内已无 `domains.scheduling`、`platform.taskdb`、`workflow_executor`、`workflow_runtime` 的代码级残留引用。
2. 静态错误检查：
- `academicresearch_kit/`：无错误。
- `autodokit/`：无错误。

## 6. 当前结论与剩余项

1. 本轮已完成：S3 收口、S4 结构迁移与 ARK 旧实现删除清单。
2. 本轮未完成：S5 全量“领域引擎事务化 + domains 删除”尚未实施。

## 7. 下一步建议

1. 按子域拆分 S5 子任务，先做 `local_reference_ingestion + knowledge_prescreen + literature_reading` 三件套 Skill 契约映射，再切换到 AOK affairs。
2. CNKI 与 retrieval_governance 子域单独开工单，先定义 AOK affair 输入/输出契约映射表，再替换 ARK skills 调用链。
3. S5 每完成一个子域，立即执行“技能回归 + 文档同步 + 旧域删除”闭环，避免累积技术债。