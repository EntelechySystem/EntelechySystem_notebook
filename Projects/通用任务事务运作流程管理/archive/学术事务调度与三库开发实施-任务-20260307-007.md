# 学术事务调度与三库开发实施-任务-20260307-007

## 1. 本轮目标

1. 将 `academicresearch_kit/main.py` 从摘要 dry-run 入口升级为第一版可执行 workflow 的执行器。
2. 为 Agents/Skills 提供正式的包调用方式与技能说明，使其通过 `academicresearch_kit` 调用能力，而不是寻找已经不存在的 `src/`。

## 2. 可行性分析

1. 当前仓库已经具备 `academicresearch_kit` 正式包、`dispatch_map.json` 派发映射以及最小 workflow 示例，可在此基础上扩展第一版串行执行器。
2. 当前 `src/` 已不存在，因此技能接入的正确方向是显式指向 `academicresearch_kit` 包接口或包装脚本。
3. `scheduler_core.DispatchExecutor` 已具备模块函数派发能力，只需补一层 workflow 编排，即可形成第一版真实执行链路。

## 3. 实施计划

1. 设计并实现最小 workflow 串行执行器。
2. 将 `main.py` 接入执行器，并保留 `dry_run` 摘要能力。
3. 为最小 workflow 补一条可执行的 dispatch 映射和处理器。
4. 新增 skill 说明与包装脚本，让 Agent 明确通过 `academicresearch_kit` 调用。
5. 执行验证并回填结果。

## 4. 实施进度

1. 已开始本轮任务文档编写。
2. 已新增 `academicresearch_kit/workflow_executor.py`，实现第一版按 `workflow.flow` 串行执行的 workflow 执行器，并将每步结果写入 `logs/run/`。
3. 已新增 `academicresearch_kit/workflow_runtime.py`，提供最小可执行处理器 `dry_run_probe_handler`，用于验证路径解析、输入目录存在性与输出目录父级存在性。
4. 已增强 `academicresearch_kit/scheduler_core/dispatch_executor.py`，新增按 `dispatch_key` 派发的执行接口，并让模块函数返回处理结果供 workflow 执行器汇总。
5. 已更新 `config/scheduler/dispatch_map.json`，新增 `dry_run_probe_v1 -> academicresearch_kit.workflow_runtime:dry_run_probe_handler` 的正式映射。
6. 已更新 `academicresearch_kit/main.py`，保留 `dry_run=True` 时的摘要能力，并在 `dry_run=False` 时接入 `WorkflowExecutor` 返回 `execution_results`。
7. 已新增 `.claude/skills/ark-workflow-executor/SKILL.md` 与 `.claude/skills/ark-workflow-executor/scripts/run_ark_workflow.py`，明确 Agents/Skills 应通过 `academicresearch_kit` 正式包或包装脚本调用，不再寻找已删除的 `src/`。
8. 已同步更新 `academicresearch_kit/docs/开发者指南.md`、`academicresearch_kit/docs/用户手册.md` 与 `docs/用户手册.md`，将主入口说明从“仅摘要”修正为“dry-run 摘要 + 非 dry-run 执行”。

## 5. 验证结果

1. 重新安装验证成功：在仓库根目录执行 `./.venv/Scripts/python.exe -m pip install -e .` 后，`academic-research-kit 0.2.1` 安装成功。
2. 编程入口验证成功：执行 `import academicresearch_kit as ark; ark.run(config_path='config/config.json', dry_run=False)` 后，返回 `execution_result_count = 1`，第一条 workflow `success = True`，第一步 `result_code = PASS`。
3. 模块入口验证成功：执行 `python -m academicresearch_kit.main --config config/config.json` 后，结构化输出中包含 `workflow_count = 1`、`missing_workflow_count = 0`、`dry_run = false`、`execution_result_count = 1`，并显示步骤 `dry_run_probe` 已通过 `academicresearch_kit.workflow_runtime:dry_run_probe_handler` 成功执行。
4. 执行结果细节验证成功：模块输出确认 `resolved_input_dir` 已解析到 `database/tasks`，`resolved_output_dir` 已解析到 `logs/dry-run`，且 `input_exists = true`、`output_parent_exists = true`、`status = PASS`。
5. Skill 包装脚本验证成功：执行 `python ./.claude/skills/ark-workflow-executor/scripts/run_ark_workflow.py --config config/config.json --dry-run` 后，输出 `workflow_count = 1`、`missing_workflow_count = 0`、`dry_run = true`、`execution_result_count = 0`，说明 skill 已正确走到正式包入口并保留 dry-run 行为。
6. 静态检查通过：`academicresearch_kit/main.py`、`academicresearch_kit/workflow_executor.py`、`academicresearch_kit/workflow_runtime.py`、`academicresearch_kit/scheduler_core/dispatch_executor.py` 与 skill 包装脚本均未发现错误。

## 6. 本轮结论

1. `academicresearch_kit/main.py` 已从“仅做 workflow 摘要”的入口升级为“dry-run 摘要 + 第一版真实执行”的正式入口。
2. 当前第一版执行器已具备最小可运行闭环：读取 `config.json`、解析 `workflow.json`、按 `flow` 串行执行、通过 `dispatch_map.json` 派发处理器、回收结构化步骤结果。
3. 面向 Agents/Skills 的接入方式已经收口到 `academicresearch_kit` 正式包与 `.claude/skills/ark-workflow-executor/` 包装脚本，不再依赖已删除的 `src/`。

## 7. 下一步建议

1. 继续为更多事务类型补充正式 `dispatch_map` 映射与处理器，逐步把第一版执行器从“最小探针”扩展到真实科研事务执行链。
2. 为 `WorkflowExecutor` 增加失败策略、停止策略和更细粒度的日志聚合，便于 workflow 规模扩大后的诊断与治理。
3. 若后续会频繁由 Agent 调用，可进一步补充 skill 示例 workflow 与调用约定，让不同技能直接复用统一包装脚本。
