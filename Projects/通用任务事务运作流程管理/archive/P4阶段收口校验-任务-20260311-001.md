# P4阶段收口校验-任务-20260311-001

## 一、任务背景与目标

- 背景：P1~P3 已完成主要迁移动作，当前进入下一阶段，需要对 S1~S5 迁移结果进行阶段收口校验，确认可进入后续迭代。
- 目标：
  - 核验 ARK 侧是否还存在已迁移技术实现残留。
  - 核验 ARK `skills/*`、`api/*` 是否存在 P3 同类类型回归风险。
  - 形成阶段完成度结论与下一阶段建议。

## 二、可行性分析

### 2.1 现状

- `academicresearch_kit/domains/` 当前仅保留空壳 `__init__.py`。
- `academicresearch_kit/api/` 已具备 `scheduler/taskdb/retrieval/local_reference_ingestion/knowledge_prescreen/literature_reading/cnki` 门面。
- 历史任务文档显示：P2、P3 已完成并通过关键文件校验。

### 2.2 主要风险

- 迁移后期常见风险：
  - 某些 skill 仍存在旧域引用（字符串或导入）。
  - 动态门面符号被继续用作静态类型表达，导致类型检查报错。

### 2.3 可行性结论

- 可行。当前改动重点是“读档核验 + 静态检查”，不需要高风险重构操作。

## 三、实施计划

1. 对照迁移蓝图核验 S1~S5 的关键路径与目录现状。
2. 检查 ARK 已迁移目录残留情况（`scheduler_core`、`tasking_db`、`domains/*` 等）。
3. 对 ARK `skills/*` 与 `api/*` 执行错误检查。
4. 对 `skills/*` 执行动态门面风险点快速扫描。
5. 汇总结论并给出下一阶段建议。

## 四、实施过程与结果

### 4.1 目录与结构核验

- `academicresearch_kit/domains/`：仅有 `__init__.py`，未见旧子域实现残留。
- `academicresearch_kit/scheduler_core/`：未发现。
- `academicresearch_kit/tasking_db/`：未发现。
- `academicresearch_kit/platform/taskdb/`：未发现。
- `academicresearch_kit/api/`：门面文件齐全，包含 `cnki.py`。

### 4.2 静态错误检查

- 检查范围：`academicresearch_kit/skills` 与 `academicresearch_kit/api`。
- 结果：`No errors found`。

### 4.3 动态门面风险扫描

- 扫描目标：`skills/*.py` 中 `cast(...)`、可能的类型表达式回归。
- 结果：未发现与 P3 同类的动态门面静态类型错误模式。

## 五、进度总结

- P4 阶段收口校验已完成。
- 当前结论：S1~S5 在 ARK 侧已达到“技术实现迁移后无残留 + 门面可用 + 静态检查通过”的进入下一阶段条件。

## 六、下一步建议

1. 进入下一阶段时，将重点从“迁移实现”转为“回归验证与文档收口”，优先补端到端回归用例。
2. 为 `cnki_bridge`、`retrieval_governance`、`local_reference_ingestion` 增加至少 1 条门面调用回归测试（execute=False/True 分支覆盖）。
3. 在阶段终稿中统一更新开发者文档，补充“ARK 门面层 + AOK 事务层”的职责边界图。
