# 学术事务调度与三库开发实施-任务-20260308-015

## 1. 需求与目标

### 1.1 需求

对当前阶段重构结果做最后一轮检查，确认：

1. 文件夹管理是否已经达到预期分层状态。
2. 若目录分层无误，相关文档是否已经同步更新。

### 1.2 目标

1. 核对 `academicresearch_kit/` 当前目录结构与阶段计划的一致性。
2. 检查 `academicresearch_kit/docs/开发者指南.md`、`academicresearch_kit/docs/API手册.md` 是否反映最新真实结构。
3. 标出已经完成收口的部分与尚未完成收口的部分。

## 2. 可行性分析

### 2.1 检查依据

1. 当前仓库中已存在阶段 A 任务文档 `...-013.md` 与阶段 B 任务文档 `...-014.md`。
2. `academicresearch_kit/` 目录下已经可直接看到 `api/`、`application/`、`platform/`、`domains/`、`scheduler_core/`、`tasking_db/` 等关键目录。
3. 文档现状可通过直接读取 `开发者指南` 与 `API 手册` 校对。

结论：可以直接通过目录与文档比对完成最终检查。

## 3. 检查过程

1. 检查 `academicresearch_kit/` 根目录与 `domains/`、`platform/` 当前子目录。
2. 核对 `academicresearch_kit/docs/开发者指南.md` 的结构说明。
3. 核对 `academicresearch_kit/docs/API手册.md` 的公开入口说明。
4. 检查 `api/taskdb.py` 当前是否仍指向旧 `tasking_db/` 路径。
5. 检查调度迁移后的核心文件是否仍有静态错误。

## 4. 检查结果

### 4.1 文件夹管理状态

当前状态分为两部分：

1. 已完成收口：
- `api/` 已建立并作为稳定门面层存在。
- `application/`、`platform/` 目录骨架已建立。
- 调度实现已迁移到 `academicresearch_kit/domains/scheduling/`。
- `academicresearch_kit/scheduler_core/` 已退化为兼容层，符合阶段 B 目标。

2. 尚未完成收口：
- `academicresearch_kit/platform/` 当前仍为空目录。
- `academicresearch_kit/tasking_db/` 仍是正式实现所在目录。
- `academicresearch_kit/api/taskdb.py` 仍直接从 `academicresearch_kit.tasking_db.*` 导入。

结论：

当前“文件夹管理”对调度域来说已经没问题，但对 taskdb 这条线来说还没有完成阶段 C 的平台化迁移，因此不能视为整套目录分层已经全部收口。

### 4.2 文档更新状态

1. `academicresearch_kit/docs/开发者指南.md`：
- 已更新阶段 A/B 信息。
- 已写明 `api/`、`application/`、`platform/` 的阶段性定位。
- 已写明 `domains/scheduling/` 是正式调度实现位置。
- 已写明 `scheduler_core/` 是兼容层。
- 仍将 `tasking_db/` 描述为当前数据平面正式实现位置，这与当前代码现状一致。

2. `academicresearch_kit/docs/API手册.md`：
- 调度 API 来源已经更新为 `academicresearch_kit.api.scheduler.*`。
- taskdb API 仍指向 `academicresearch_kit.api.taskdb.*`，而该门面目前仍合法转发到旧 `tasking_db/` 实现，因此文档与现状一致。

结论：

相关文档对“当前真实代码状态”是已更新且基本一致的；只是因为 taskdb 迁移尚未实施，所以文档中还没有也不应该提前写入 `platform/taskdb` 的最终结构。

### 4.3 静态检查状态

本轮复核的调度相关关键文件错误检查通过，包括：

1. `academicresearch_kit/api/scheduler.py`
2. `academicresearch_kit/main.py`
3. `academicresearch_kit/workflow_executor.py`
4. `academicresearch_kit/skills/scheduler_dispatch.py`
5. `academicresearch_kit/cli/scheduler.py`
6. `academicresearch_kit/domains/retrieval_governance/engine.py`
7. `academicresearch_kit/tasking_db/replay_engine.py`

## 5. 最终结论

1. 如果只看阶段 A/B 范围，那么目录管理没有问题，相关文档也已经同步更新。
2. 如果看整套重构总计划，那么目前还不能说“全部文件夹管理都没问题”，因为 `tasking_db -> platform/taskdb` 的阶段 C 尚未完成。
3. 文档当前没有明显遗漏；它们准确反映的是“已完成到阶段 B”的代码状态，而不是尚未实施的目标状态。

## 6. 下一步建议

1. 若要完成整套目录收口，下一步应执行阶段 C：将 `tasking_db/` 平台化迁移到 `platform/taskdb/`，并保留旧路径兼容层。
2. 阶段 C 完成后，再统一更新 `开发者指南` 和 `API手册`，把 taskdb 的正式实现位置改写为新路径。
