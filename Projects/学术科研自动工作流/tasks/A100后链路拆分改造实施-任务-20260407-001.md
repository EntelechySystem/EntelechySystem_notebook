# A100后链路拆分改造实施-任务-20260407-001

## 1. 预处理

### 1.1 输入依据

本轮实施基于以下已确认文档：

1. `design-A100后链路拆分重构-20260407-001.md`
2. `plan-A100后链路拆分重构-20260407-001.md`

### 1.2 执行范围约束

本轮严格遵循以下边界：

1. 不改动 A080、A090、A095。
2. 只改动 A100 及其后续链路。

## 2. 实施任务

### 2.1 A100 事务收缩（代码）

实施内容：

1. 将 A100 事务改为“文献精解析资产化”。
2. 保留 `non_review_deep` 解析资产生成与校验。
3. 移除 A100 对标准文献笔记正文写作职责。
4. 移除 A100 对五类分析笔记正式修订职责。
5. 移除 A100 对创新点笔记更新职责。
6. 状态推进调整为 `deep_read_decision=parse_ready` 后移交 A105。

变更文件：

1. `autodokit/affairs/文献研读与正式知识回写/affair.py`

### 2.2 新增 A105 事务（代码）

实施内容：

1. 新增 A105 事务文件。
2. A105 消费 `deep_read_decision='parse_ready'` 且 `deep_read_done=0` 的条目。
3. A105 执行批判性研读并生成标准文献笔记。
4. A105 执行新候选回流。
5. A105 回写 `deep_read_done=1` 与 `deep_read_count += 1`。

变更文件：

1. `autodokit/affairs/文献批判性研读与标准笔记/affair.py`

### 2.3 Agent 编排与职责文案同步

实施内容：

1. 更新 A100 agent 文案，改为“仅精解析资产化”。
2. 新增 A105 agent 文案，定义批判性研读职责。
3. 更新 PA 编排 agent：新增 A105 入口映射与主链顺序。

变更文件：

1. `~/.copilot/agents/ar_A100_文献研读与正式知识回写事务智能体_v6.agent.md`
2. `~/.copilot/agents/ar_A105_文献批判性研读与标准笔记事务智能体_v1.agent.md`
3. `~/.copilot/agents/ar_PA项目经理编排智能体_v5.agent.md`

### 2.4 快速验证

执行了 Python 语法级验证：

1. `compileall` 检查 A100 与 A105 affairs。
2. 问题检查工具确认两文件无错误。

## 3. 本轮结果总结

### 3.1 已完成

1. A100 已完成单一职责收缩。
2. A105 已新增并可编译通过。
3. A100/A105/PA agent 文案与编排关系已同步。
4. 本轮未触碰 A080-A095 的代码与职责。

### 3.2 当前状态

1. 代码层已具备 A100 -> A105 的职责拆分骨架。
2. 调度映射文案已体现 A105 在 A100 之后、A110 之前。
3. 尚未执行端到端事务实跑回归。

## 4. 下一步建议

1. 补齐 A105 对应的 affairs_config 与 PA 默认 config 节点注册（若当前运行配置尚未包含 A105）。
2. 做一次真实样本端到端回归：A095 -> A100 -> A105 -> A110。
3. 将 A110/A120/A130/A140 的“正式修订职责迁移”做第二轮代码落地，并同步 API 手册。
