# 学术科研工作流关键阶段开发-任务-20260325-001

## 一、预处理

### 1. 输入依据

- 分析文档：`analyse-学术科研工作流全流程映射与实现对照-20260324-001`
- 计划文档：`plan-学术科研工作流补缺补漏实施计划-20260324-001`
- 设计文档：`design-学术科研工作流补缺补漏优先级设计-20260324-001`

### 2. 本轮实施目标（关键阶段）

根据“主链优先 + 阻断缺口优先”，先实施阶段 2 的核心阻断项：

- 新增 AOK 事务：`单篇粗读`
- 打通“粗读产物可进入精读”的最小可用输入输出结构
- 同步 API 文档，保证源码行为与手册一致

### 3. 本轮计划步骤

1. 对照现有 `单篇精读` 与 `语义预筛选` 的事务结构，确定最小可运行实现。
2. 新建 `autodokit/affairs/单篇粗读/` 三件套并实现 `execute(config_path)`。
3. 补充 `docs/API手册.md` 的事务契约说明与字段表。
4. 进行静态检查（errors）并记录结果。

## 二、实施任务

### 1. 已完成代码与文档变更

#### 1.1 新增事务目录与文件

- `autodokit/affairs/单篇粗读/__init__.py`
- `autodokit/affairs/单篇粗读/affair.py`
- `autodokit/affairs/单篇粗读/affair.json`
- `autodokit/affairs/单篇粗读/affair.md`

#### 1.2 事务实现要点

- 入口函数：`execute(config_path: Path) -> list[Path]`
- 关键行为：
  - 从 `docs.jsonl` 按 `doc_id` 或 `uid` 读取目标文献；
  - 自动提取参考文献段落行；
  - 可选将参考文献写入文献数据库占位引文；
  - 输出粗读笔记 `rough_reading_{uid_or_doc_id}.md`；
  - 输出结构化结果 `rough_reading_result_{uid_or_doc_id}.json`。
- 路径约束：
  - `input_docs_jsonl`、`output_dir`、`bibliography_csv`（若提供）均要求绝对路径。

#### 1.3 API 手册同步

已同步更新：

- `docs/API手册.md`
  - 在文献工具说明中补充 `单篇粗读` 已切换新字段契约；
  - 新增 “3.4 单篇粗读（autodokit.affairs.单篇粗读.affair）”章节；
  - 增加字段表、输出文件与异常说明。

## 三、实施结果

### 1. 本轮产出

- 阻断项“单篇文献粗读无同名事务”已完成最小可运行落地。
- 粗读与精读之间形成可交接结构（粗读笔记 + JSON 结果 + 引文占位回写）。
- API 文档已与本次源码真实行为同步。

### 2. 当前状态判断

- 状态：`关键阶段首项已落地（可继续批次 A 其余项）`
- 风险：
  - 目前粗读为“规则提取 + 占位回写”的轻量实现，后续可再加入 LLM 粗读增强。

## 四、下一步建议

1. 继续批次 A：完成“研究问题与文献入口链（DF01~DF04）”的字段契约统一（关键词生成 → 检索治理 → 语义预筛选）。
2. 增加 `单篇粗读` 的最小回归样例（docs.jsonl 小样本 + bibliography.csv），纳入事务验证脚本。
3. 在分析表中将 `单篇文献粗读` 对应项从“未建立同名事务”更新为“已建立高置信对应”。
