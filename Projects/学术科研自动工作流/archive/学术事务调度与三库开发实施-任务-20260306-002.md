# 学术事务调度与三库开发实施-任务-20260306-002

## 1. 本轮目标

在上一轮纯 Python 原型实现基础上，将 `src/` 内能力重新封装为可稳定导入的 `academicresearch_kit` 包，并为 Agents/Skills 补齐独立 CLI 入口，再对各 CLI 做最小运行验证。

## 2. 可行性分析

1. 当前 `src/` 已具备调度、任务数据库、检索治理三类实现，可通过封装层稳定导出。
2. 通过新增顶层包而不是直接迁移源码，可以降低对既有实现的扰动。
3. 使用 `pyproject.toml` 的 `project.scripts` 可直接生成 `ark-*` 命令，适合 Skills 调用。

## 3. 实施步骤

1. 新建 `academicresearch_kit/` 包及其 CLI 目录。
2. 用稳定导出层封装 `src` 中的核心能力。
3. 新增检索治理兼容模块路径，供调度映射和 CLI 复用。
4. 更新 `pyproject.toml` 中的脚本入口。
5. 更新默认派发映射到 `academicresearch_kit.retrieval_governance.engine`。
6. 重装可编辑包并逐个执行 CLI 验证。

## 4. 当前进展

### 4.1 已完成编码

1. 新增 `academicresearch_kit/__init__.py`。
2. 新增 `academicresearch_kit/scheduler.py`、`academicresearch_kit/taskdb.py`、`academicresearch_kit/retrieval.py`。
3. 新增 `academicresearch_kit/retrieval_governance/__init__.py`、`academicresearch_kit/retrieval_governance/engine.py`。
4. 新增 CLI：
   - `academicresearch_kit/cli/scheduler.py`
   - `academicresearch_kit/cli/taskdb_bootstrap.py`
   - `academicresearch_kit/cli/taskdb_validate.py`
   - `academicresearch_kit/cli/taskdb_replay.py`
   - `academicresearch_kit/cli/retrieval.py`
5. 更新 `pyproject.toml` 增加 `ark-*` 命令。
6. 更新 `src/tasking_db/bootstrap.py` 与 `config/scheduler/dispatch_map.json` 的模块路径。

### 4.2 待完成

1. 重装包。
2. 逐个运行 CLI。
3. 汇总运行结果。

## 5. CLI 验证结果

### 5.1 安装验证

1. 已重新执行 `uv pip install --python .\\.venv\\Scripts\\python.exe -e .`。
2. 新增的 `ark-*` 命令已成功生成并可直接调用。

### 5.2 命令验证

1. `ark-taskdb-bootstrap --project-root .`
   - 结果：成功。
   - 输出：`已完成模板初始化`。

2. `ark-taskdb-validate --project-root .`
   - 结果：成功。
   - 输出：`error_count = 0`。

3. `ark-taskdb-replay --project-root .`
   - 结果：成功。
   - 输出：空对象 `{}`，符合当前尚无执行日志的预期。

4. `ark-retrieval --query "bank systemic risk" --object-type literature --source-type online --region-type global --access-type open`
   - 结果：成功。
   - 输出：生成 `bundle`，`permission_status=approved`，`result_code=PASS`，后续节点为 `reference_ingestion`。

5. `ark-scheduler --project-root . --task-uid cli-demo --query "bank systemic risk" --execute`
   - 结果：成功。
   - 输出：生成完整调度事件，包含候选评分、守卫决策和纯 Python 派发回执。

## 6. 本轮结论

1. `src/` 已成功封装为稳定导入名 `academicresearch_kit`。
2. 当前建议在 Skills/Agents 中使用：`import academicresearch_kit as ark`。
3. 现有原型功能已经具备独立 CLI 入口，可直接被自动化脚本调用。
4. 当前发行名仍是 `academic-research-toolbox`，但导入名和 CLI 均已切换到 `ark` 语义层。
