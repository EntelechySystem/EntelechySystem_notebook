# ARK全功能独立干测试-任务-20260308-006

## 需求与目标

- 按“先开发不调试”要求，先实现以下三个 blocked skill 的可执行入口：
  - `aliyun-bailian-sdk-bootstrap`
  - `cn-web-acquisition`
  - `econometrics-runtime-setup`
- 暂不运行回归，不刷新统计报告。

## 可行性分析

- 三项能力均可先以 MVP 形式提供“输入校验 + 结构化结果”能力。
- 通过接入 `skills/__init__.py` 与全量干测脚本，可在后续阶段直接开启回归验证。
- 由于本轮不调试，风险主要是运行时兼容性风险，留到下一轮处理。

## 计划步骤

1. 新增 3 个 skill 文件并实现 `run_*_skill` 入口。
2. 更新 `skills/__init__.py`。
3. 更新全量干测脚本：新增 case 并从 `UNSCRIPTED_SKILLS` 移除对应项。
4. 文档记录本轮仅开发未调试。

## 实施记录

- 新增 `academicresearch_kit/skills/aliyun_bailian_sdk_bootstrap.py`，实现 `run_aliyun_bailian_sdk_bootstrap_skill`。
- 新增 `academicresearch_kit/skills/cn_web_acquisition.py`，实现 `run_cn_web_acquisition_skill`。
- 新增 `academicresearch_kit/skills/econometrics_runtime_setup.py`，实现 `run_econometrics_runtime_setup_skill`。
- 更新 `academicresearch_kit/skills/__init__.py`，导出上述 3 个新入口。
- 更新 `academicresearch_kit/demos/run_ark_full_feature_drytests.py`：
  - 新增 3 条 skill 测试用例；
  - 从 `UNSCRIPTED_SKILLS` 移除 `aliyun-bailian-sdk-bootstrap`、`cn-web-acquisition`、`econometrics-runtime-setup`。
- 本轮按要求未执行回归测试、未刷新报告统计。

## 结果与进度总结

- 三个目标能力已完成脚本化开发并完成代码接入。
- 当前代码已具备下一轮直接回归验证条件。
- 统计口径暂未更新（因本轮明确“先开发不调试”）。

## 下一步建议

1. 下一轮执行全量回归，验证本轮新增 3 项是否全部通过。
2. 再结合 `cnki-skills-master` 设计 `cn-subscribed-literature-access` 的实现方案。
