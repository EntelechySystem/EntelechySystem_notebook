# 学术事务调度与三库开发实施-任务-20260306-005

## 1. 本轮目标

将弃用目录 `academic_research_toolbox/` 中仍有价值的功能迁移到正式工具包 `academicresearch_kit/`，并继续确认 `src/` 是否仍有残留；迁移完成后以 `academicresearch_kit` 作为唯一正式包入口，对相关功能执行 dry-run 验证。

## 2. 可行性分析

1. 当前 `src/` 已不存在，因此本轮重点只剩 `academic_research_toolbox/` 的兼容壳与入口逻辑。
2. `academic_research_toolbox/main.py` 当前只承载最小 `run/main` dry-run 摘要逻辑，迁移成本低，适合并入 `academicresearch_kit`。
3. 只要同步更新 `academicresearch_kit/__init__.py` 与 `pyproject.toml` 的主脚本指向，就可以把正式入口完全切换到 `academicresearch_kit`。

## 3. 初步核对

1. `academic_research_toolbox/` 当前仅包含：`__init__.py`、`main.py` 和一个空的 `tools/` 目录。
2. `src/` 当前已不存在，无需进一步迁移。
3. 当前 `academicresearch_kit/__init__.py` 仍依赖 `academic_research_toolbox` 导出 `run/main`。
4. 当前 `pyproject.toml` 的主命令 `academic-research-kit` 仍指向 `academic_research_toolbox.main:main`。

## 4. 实施计划

1. 将 `academic_research_toolbox.main` 的 `run/main` 能力迁移到 `academicresearch_kit`。
2. 更新 `academicresearch_kit/__init__.py` 与 `pyproject.toml`，让正式导入和主命令都仅依赖 `academicresearch_kit`。
3. 尽可能删除弃用目录 `academic_research_toolbox/`。
4. 对迁移后的主入口执行 dry-run 验证并记录结果。

## 5. 实施进度

1. 已完成弃用目录现状核对，确认当前可迁移内容集中在 `academic_research_toolbox/main.py`。
2. 已新建 `academicresearch_kit/main.py`，将旧包中的 `resolve_config_path`、`load_json_file`、`resolve_workspace_root`、`summarize_workflow`、`run`、`main` 迁移为正式入口实现。
3. 已更新 `academicresearch_kit/__init__.py`，改为通过懒加载方式导出 `run/main`，使 `import academicresearch_kit as ark` 与 `python -m academicresearch_kit.main` 可同时成立。
4. 已更新 `pyproject.toml`，将主命令 `academic-research-kit` 切换到 `academicresearch_kit.main:main`，同时移除对弃用包 `academic_research_toolbox` 的打包配置。
5. 已补充 `config/config.json` 作为最小 dry-run 配置，默认指向仓库根目录并保持空 `run` 列表，便于做入口验证。
6. 已删除弃用目录 `academic_research_toolbox/` 与旧元数据目录 `academic_research_toolbox.egg-info/`。
7. 已同步更新关键文档中的主入口示例，包括 `README.md` 与 `docs/用户手册.md`。

## 6. 验证结果

1. 可编辑安装成功：在 `.venv` 中执行 `python -m pip install -e .` 完成重新安装。
2. 导入验证成功：执行 `import academicresearch_kit as ark` 后，`callable(ark.run)` 返回 `True`。
3. 新主入口 dry-run 成功：

```json
{
	"config_path": "C:\\Users\\Ethan\\CoreFiles\\ProjectsFile\\AcademicResearch-auto-workflow\\config\\config.json",
	"workspace_root": "C:\\Users\\Ethan\\CoreFiles\\ProjectsFile\\AcademicResearch-auto-workflow",
	"workflow_count": 0,
	"missing_workflow_count": 0,
	"workflows": [],
	"missing_workflows": [],
	"dry_run": true,
	"parallel": false,
	"max_workers": 2,
	"log_dir": ""
}
```

4. 控制台脚本 `academic-research-kit --config config/config.json --dry-run` 输出与模块入口一致。
5. 关键 CLI 烟雾测试通过：`ark-scheduler --help`、`ark-taskdb-bootstrap --help`、`ark-retrieval --help` 均可正常显示参数帮助。

## 7. 结果总结

1. `academicresearch_kit` 已成为仓库内唯一正式 Python 包入口，旧包 `academic_research_toolbox` 已完成迁移并删除。
2. 当前已确认 `src/` 无残留、旧 `tools/` 无可迁移源码，仅剩的有效逻辑已全部并入 `academicresearch_kit`。
3. 迁移后的入口已完成 dry-run 与基础 CLI 烟雾验证，满足本轮“先做干测试、不展开业务执行”的要求。

## 8. 下一步建议

1. 若后续需要真正执行 workflow，可补充一个最小可运行的 `demos/workflows/.../workflow.json`，再把 `config/config.json.run` 指向它进行端到端 dry-run。
2. 仓库内仍有部分历史镜像文档位于 `academicresearch_kit/docs/`，后续可专门开一轮文档清理任务，统一移除已失效的旧目录说明。
