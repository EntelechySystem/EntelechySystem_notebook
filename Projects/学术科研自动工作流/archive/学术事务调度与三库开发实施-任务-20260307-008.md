# 学术事务调度与三库开发实施-任务-20260307-008

## 1. 本轮目标

1. 盘点 `AcademicResearch-auto-workflow` 中各个 skill 的脚本入口，并检查是否能够正常使用。
2. 在不依赖 `opencode` 的前提下，直接通过正式包入口或 skill 包装脚本让最小 workflow 流程自行走一遍。

## 2. 可行性分析

1. 当前技能目录集中在 `.claude/skills/`，可以先按目录和脚本文件两层盘点真实存在的脚本入口。
2. 由于上一轮已经补齐 `academicresearch_kit` 正式执行入口和 `ark-workflow-executor` skill 包装脚本，因此至少最小 workflow 已具备脱离 `opencode` 的独立执行条件。
3. 若多数 skill 仅有说明文档而没有 `scripts/` 目录，则本轮重点应转为确认哪些 skill 真有脚本、哪些 skill 当前只能作为提示模板使用。

## 3. 实施计划

1. 枚举 `.claude/skills/` 下各 skill 的实际脚本文件。
2. 对存在脚本的 skill 做最小可执行检查。
3. 选择可执行入口，直接绕开 `opencode` 跑一次最小 workflow。
4. 回填检查结果、可用性结论和后续建议。

## 4. 实施进度

1. 已创建本轮任务文档。
2. 已盘点 `.claude/skills/` 下各 skill 的脚本文件，当前仓库中真正存在的脚本入口只有 `.claude/skills/ark-workflow-executor/scripts/run_ark_workflow.py`。
3. 已确认其余 skill 当前仅提供 `SKILL.md` 说明文档，不包含可直接执行的脚本文件，因此它们现阶段更适合作为提示模板或流程规范，而不是命令式脚本入口。
4. 已使用项目 `.venv` 对 `run_ark_workflow.py` 做最小可执行检查，并准备绕开 `opencode` 直接实跑最小 workflow。

## 5. 检查结果

1. 脚本清点结果：当前 `AcademicResearch-auto-workflow` 内只有 `ark-workflow-executor` 这个 skill 带有实际脚本文件。
2. 帮助信息验证成功：执行 `./.venv/Scripts/python.exe ./.claude/skills/ark-workflow-executor/scripts/run_ark_workflow.py --help` 后，参数说明正常输出，`--config` 与 `--dry-run` 参数可见。
3. dry-run 验证成功：执行 `./.venv/Scripts/python.exe ./.claude/skills/ark-workflow-executor/scripts/run_ark_workflow.py --config config/config.json --dry-run` 后，返回 `workflow_count = 1`、`missing_workflow_count = 0`、`dry_run = true`、`execution_result_count = 0`。
4. 真实执行验证成功：执行 `./.venv/Scripts/python.exe ./.claude/skills/ark-workflow-executor/scripts/run_ark_workflow.py --config config/config.json` 后，返回 `dry_run = false`、`execution_result_count = 1`，且步骤 `dry_run_probe` 的 `result_code = PASS`、`success = true`。
5. 正式控制台脚本验证成功：执行 `./.venv/Scripts/academic-research-kit.exe --config config/config.json` 后，输出与 skill 包装脚本一致，说明当前最小 workflow 可以在不依赖 `opencode` 的情况下直接由正式包入口自行走完。
6. 运行日志验证成功：执行完成后，`logs/run/workflow_最小dry_run_execution.jsonl` 已生成并追加步骤记录，说明 workflow 执行链路已经落盘。
7. 环境检查补充：`configure_python_environment` 当前未识别到项目 `.venv`，只返回系统 `C:/Python314/python.exe`；考虑到该环境缺少项目依赖，本轮实际验证沿用了仓库内已验证可用的 `.venv`。
8. 编码观察：直接在 PowerShell 中读取 `logs/run/workflow_最小dry_run_execution.jsonl` 时，中文字段出现控制台编码乱码；但日志文件已正常生成，不影响执行结果本身，后续若要人工查看可优先使用 UTF-8 方式读取。

## 6. 本轮结论

1. 以“脚本可执行性”来看，当前仓库并不是所有 skill 都带脚本；真正可直接运行的只有 `ark-workflow-executor`。
2. `ark-workflow-executor` 当前已经可以作为不依赖 `opencode` 的独立流程入口使用，既能 dry-run，也能真实执行最小 workflow。
3. 现阶段其余 skill 更接近“能力说明文档”，并未落成可直接调用的脚本资产，因此不能按脚本入口标准统一验收为“可运行”。

## 7. 下一步建议

1. 若希望“各 skill 都能正常使用”更接近可执行层面，应为高优先级 skill 补对应 `scripts/` 包装脚本，而不只保留 `SKILL.md`。
2. 可继续沿用 `ark-workflow-executor` 的模式，为 `project-bootstrap`、`api-data-fetch`、`rag-evidence-synthesis` 等 skill 提供最小 CLI 或 Python 包装脚本。
3. 若后续需要稳定查看运行日志中的中文字段，建议补统一的 UTF-8 日志查看命令或直接在脚本中增加日志读取辅助工具。
