# P5回归验证与文档收口-任务-20260311-001

## 一、任务背景与目标

- 背景：P2（检索治理迁移）、P3（CNKI 子域迁移）、P4（阶段收口校验）已完成，当前进入下一阶段，需要补齐最小回归验证并完成文档收口。
- 目标：
  - 在无现成 `tests/` 目录前提下，建立可重复执行的最小 smoke 回归脚本。
  - 覆盖已迁移关键门面链路：`retrieval_governance`、`cnki_bridge`、`local_reference_ingestion`、`knowledge_prescreen`、`literature_reading`。
  - 输出阶段结论与下一步建议。

## 二、可行性分析

### 2.1 当前现状

- ARK 侧关键能力已门面化到 AOK 事务层。
- 工作区未发现现成测试目录（ARK/AOK 均无 `tests/`）。
- 适合采用“脚本化最小回归 + 文档化验证结果”的方式先建立验证基线。

### 2.2 风险与应对

- 风险 1：`execute=True` 分支依赖浏览器与人工交互，自动回归不稳定。
  - 应对：本轮 smoke 统一覆盖 `execute=False` 或纯内存输入路径，确保 CI/本地可稳定复现。
- 风险 2：本地环境未安装 `autodo-kit` 时门面导入失败。
  - 应对：回归脚本内增加同级仓库路径兜底（仅用于开发验证，不改变业务运行逻辑）。

### 2.3 可行性结论

- 可行。通过最小脚本已可验证“ARK 门面 -> AOK 事务”主链路可用性。

## 三、实施计划

1. 读取并确认 5 条目标 skill 的入参与输出结构。
2. 新增 P5 最小回归脚本，覆盖 5 条迁移链路。
3. 执行脚本并收集结果。
4. 输出阶段结论与下一步建议。

## 四、实施过程与结果

### 4.1 新增回归脚本

- 新增文件：`scripts/p5_migration_regression.py`
- 覆盖用例：
  - `retrieval_governance_open`
  - `cnki_search_plan_mode`
  - `local_reference_ingestion_empty`
  - `knowledge_prescreen_inline_items`
  - `literature_reading_candidates`
- 校验规则：统一校验返回结构包含 `status`、`mode`、`result` 三个关键字段。

### 4.2 执行验证

- 执行命令：
  - `C:/Users/Ethan/CoreFiles/ProjectsFile/AcademicResearch-auto-workflow/.venv/Scripts/python.exe scripts/p5_migration_regression.py`
- 执行结果：
  - `total=5`，`passed=5`，`failed=0`
  - 五条迁移链路 smoke 全部通过。

### 4.3 文档同步判断

- 本轮仅新增验证脚本，不涉及 API 类/函数/参数契约变更。
- 结论：不触发 `docs/API手册.md` 的强制同步更新条件。

## 五、阶段进度总结

- P5 已完成“回归验证基线”落地。
- 当前状态：S1~S5 迁移在最小可执行验证口径下通过，具备进入后续增强阶段（真实执行链路与自动化测试体系建设）的条件。

## 六、下一步建议

1. 新增 `execute=True` 的受控回归（可选人工闸门），重点覆盖 `cnki_bridge` 浏览器执行分支。
2. 补建正式 `tests/` 目录与 `pytest` 用例，将 `scripts/p5_migration_regression.py` 逐步拆分为可维护单测。
3. 在阶段终稿中补充“ARK 门面层 + AOK 事务层”职责边界与回归清单，作为后续变更准入检查项。
