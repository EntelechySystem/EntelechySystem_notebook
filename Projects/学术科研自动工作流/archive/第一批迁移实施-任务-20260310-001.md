# 第一批迁移实施-任务-20260310-001

## 1. 需求与目标

- 需求：根据《ARK 技术层迁移 AOK 实施蓝图 V1》启动第一批迁移。
- 目标：先完成低风险、可立即落地的兼容层清理动作，并保证现有正式 API 路径不受影响。
- 范围：仅处理迁移工程相关代码与文档；`data/raw/` 的本地文献 PDF/bib 数据不纳入本轮迁移处理。

## 2. 可行性分析

- 现状判断：`academicresearch_kit/scheduler_core` 与 `academicresearch_kit/tasking_db` 为兼容壳层目录，核心能力已在正式路径（`domains/scheduling`、`platform/taskdb`）提供。
- 风险评估：直接删除兼容壳层会有导入路径断裂风险。
- 风险缓解：先做全仓导入检索，确认无 Python 代码继续依赖 `academicresearch_kit.scheduler_core` 与 `academicresearch_kit.tasking_db`，再执行删除。
- 可行性结论：可执行，且符合项目“不保留过时代码”的开发约定。

## 3. 计划步骤

1. 盘点第一批目标模块与依赖引用。
2. 删除兼容壳层文件与目录。
3. 复查代码引用，确认无残留导入。
4. 同步本轮任务文档，记录结果与后续建议。

## 4. 实施记录

### 4.1 已完成事项

- 已删除 `academicresearch_kit/scheduler_core/` 下全部兼容壳层文件。
- 已删除 `academicresearch_kit/tasking_db/` 下全部兼容壳层文件。
- 已清理两个目录下的兼容壳层源码文件，当前仅残留 `__pycache__/` 目录（不参与运行时导入）。
- 已完成代码检索：未发现 `academicresearch_kit.scheduler_core`、`academicresearch_kit.tasking_db` 的 Python 导入残留。

### 4.2 本轮执行约束

- 已按用户说明忽略 `data/raw/` 文献数据改动，不将其纳入迁移判断与处理。
- 迁移操作聚焦代码层，不触碰用户本地文献资产。

### 4.3 S2 实施进展（20260311）

- 已在 AOK 新增统一工具模块：
	- `autodokit/tools/project_runtime.py`
	- `autodokit/tools/runtime_trace.py`
- 已在 AOK `autodokit/tools/__init__.py` 导出上述工具符号。
- 已在 ARK 删除本地重复实现：
	- `academicresearch_kit/tools/project_runtime.py`
	- `academicresearch_kit/tools/flow_trace.py`
- 已将 ARK 工具入口改为统一代理到 AOK：
	- 新增 `academicresearch_kit/tools/aok_runtime_proxy.py`（懒加载代理）
	- `academicresearch_kit/tools/__init__.py` 改为从代理层导入。
- 已在 ARK `pyproject.toml` 增加依赖：`autodo-kit>=0.3.1`。
- 静态检查结果：本次改动涉及文件未发现语法/类型错误。

### 4.4 S3 实施进展（20260311）

- 已在 AOK 新建调度模块目录：`autodokit/scheduling/`。
- 已完成 ARK 调度模块首轮迁入 AOK（`candidate_builder.py`、`dispatch_executor.py`、`edge_scorer.py`、`event_logger.py`、`route_guard.py`、`route_selector.py`、`service.py`、`types.py`、`__init__.py`）。
- 已完成 AOK 调度模块内部导入命名空间改造：`academicresearch_kit.domains.scheduling.*` -> `autodokit.scheduling.*`。
- 已将 ARK 调度门面切换到 AOK：`academicresearch_kit/api/scheduler.py` 改为动态代理 `autodokit.scheduling` 导出。
- 风险处置：迁移过程中出现过一次批量替换导致编码损坏，已通过覆盖回滚并改为逐文件补丁修复，当前文件内容正常。
- 兼容状态：`scheduler_dispatch` 仍暂时使用 ARK 本地 `types` 作为类型导入来源，以保证当前开发环境静态检查通过；运行时调度类已由 AOK 提供。

### 4.5 S3 运行链接入进展（20260311）

- 已在 AOK `autodokit/flow_graph/node_runtime_v2.py` 接入 `autodokit.scheduling.SchedulerService`。
- 当前策略为“最小侵入接入”：在每个节点正式执行前，先调用一次 `dispatch_once` 生成调度决策事件，再继续节点处理。
- 已新增运行时 trace 事件：`node_scheduled`，用于记录选中节点、选择策略与选择理由。
- 已接入 AOK 调度事件日志：`logs/run/aok_scheduler_events.jsonl`（通过 `DispatchEventLogger` 输出）。
- 兼容性说明：本次接入保持既有拓扑执行主路径不变（避免行为回归），属于“先接入、后策略增强”阶段。

### 4.6 S3 去耦合收口进展（20260311）

- 已在 AOK `autodokit/scheduling/__init__.py` 补充调度类型导出（`ResultCode`、`DispatchKind`、`SchedulerContext`、`CandidateEdge` 等）。
- 已在 ARK `academicresearch_kit/api/scheduler.py` 扩展类型门面导出，统一由 AOK 调度模块代理提供。
- 已在 ARK 业务模块完成导入迁移，移除非调度域目录对 `academicresearch_kit.domains.scheduling.types` 的运行时依赖：
	- `academicresearch_kit/cli/scheduler.py`
	- `academicresearch_kit/skills/scheduler_dispatch.py`
	- `academicresearch_kit/platform/taskdb/state_machine.py`
	- `academicresearch_kit/platform/taskdb/replay_engine.py`
	- `academicresearch_kit/domains/retrieval_governance/engine.py`
	- `academicresearch_kit/workflow_executor.py`
- 静态检查结果：上述迁移文件当前无错误。

## 5. 进度总结

- 第一批迁移中的兼容壳层源码清理子任务已完成；目录层面仍有 `__pycache__/` 残留，可在后续统一缓存清理时处理。
- 第一批后续 S2（基础工具归一）已完成首轮落地：AOK 成为 `project_runtime/flow_trace` 的唯一实现源，ARK 仅保留代理入口。
- 第一批后续 S3 已完成“模块迁入 + 门面切换 + 运行链最小接入 + 类型门面去耦合”，下一步进入一致性验证与旧域删除评估。
- 当前代码结构更贴合正式实现路径，降低后续维护与误导入成本。

## 6. 下一步建议

1. 执行最小回归验证：在 ARK 环境中安装/可见 `autodo-kit` 后，验证 `main.py`、`cli/scheduler.py`、`skills/scheduler_dispatch.py` 的关键路径导入与运行。
2. 继续推进 S3：补齐一致性验证（结果、回放、日志字段）并对比接入前行为差异。
3. 一致性验证通过后，执行 ARK `domains/scheduling/*` 删除与残留引用清理。
