# ARK未接入AOK事务技能收口计划-计划-20260311-001

## 一、目标

- 基于 `analyse-ARK未从AOK事务接入技能清单-20260311-001.md`，分批将 ARK 中未接入 AOK `affairs` 的技能完成分流与收口。
- 保持 ARK 外部调用契约稳定，遵循“ARK 门面编排 + AOK 事务实现”的单一实现源策略。
- 新增硬约束：`academicresearch_kit/skills/*.py` 全量通过 AOK 事务能力执行，不再保留 ARK 本地业务实现。

## 0.1 安装与导入约定（新增）

- 在 `AcademicResearch-auto-workflow` 仓库执行本地 editable 安装：
  - `pip install -e "C:\Users\Ethan\CoreFiles\ProjectsFile\autodo-kit"`
- Python 导入约定：
  - 使用 `autodokit` 包名导入（示例：`from autodokit.affairs.检索治理 import RetrievalGovernanceEngine`）。
  - 说明：`autodo-kit` 是仓库目录名，不是 Python 导入名；代码中统一使用 `autodokit`。
- 调用方式约定：
  - 优先使用 AOK 事务提供的引擎类/函数直接调用。
  - 需要文件化运行时，使用对应事务 `execute(config_path)`，允许绕过流程图直接执行事务。

## 二、范围

- 范围内：
  - `academicresearch_kit/skills/*.py` 全量技能（含已接入与未接入项）统一对齐到 AOK 事务调用口径。
  - `academicresearch_kit/api/*.py` 新增或扩展必要门面。
  - AOK 缺失事务的补建（`autodokit/affairs/<事务名>/affair.py/json/md`）。
  - 对应最小回归用例与任务文档同步。
- 范围外：
  - 无（原先 `scheduling` / `taskdb` 非 affairs 路径也纳入事务化整改范围）。

## 三、分阶段计划

### P0：治理与证据主干先行

- 目标技能：
  - `method_whitelist_selection`
  - `integrity_check`
  - `rag_evidence_synthesis`
- 执行动作：
  - 在 AOK 新增对应事务目录与三件套（`affair.py/json/md`）。
  - 在 ARK 新增 `api` 门面并切换技能调用路径。
  - 增加 smoke 回归用例（`tests/`）覆盖门面调用。
- 验收标准：
  - 技能入口不改名；调用结果结构保持兼容。
  - ARK 侧无旧实现残留导入。

### P1：实证分析流水线收口

- 目标技能：
  - `did_rdd_pipeline`
  - `empirical_four_pack`
  - `variable_operationalization`
- 执行动作：
  - 抽取可复用核心到 AOK tools，再封装为 affairs。
  - ARK 技能改为门面式调用，保留参数兼容层。
- 验收标准：
  - 事务参数契约在 `affair.md` 明确。
  - 至少 1 条成功路径回归测试通过。

### P1：写作闭环能力收口

- 目标技能：
  - `paper_draft`
  - `revision_response`
- 执行动作：
  - 在 AOK 形成写作事务模板与输入输出契约。
  - ARK 技能切换到 AOK 事务门面。
- 验收标准：
  - 产物路径与结构稳定；失败语义可追踪。

### P2：工程支撑能力归位

- 目标技能：
  - `workspace_sanity_check`
  - `project_bootstrap`
  - `model_routing_dispatch`
- 执行动作：
  - 评估是保留 ARK 编排层还是下沉 AOK 事务。
  - 对保留项补“不迁移说明”，避免职责漂移。
- 验收标准：
  - 每项能力都明确归属与边界。

## 四、执行节奏

1. 每轮处理 2-4 个技能，执行“补事务/复用事务-切门面-删本地实现-回归-文档”闭环。
2. 每轮新增一份 `task-*.md` 记录可行性、改动与验证。
3. 每轮结束更新 `analyse-ARK未从AOK事务接入技能清单-20260311-001.md` 状态栏。
4. 每轮完成后在 ARK 仓库执行一次 `pip install -e "C:\Users\Ethan\CoreFiles\ProjectsFile\autodo-kit"` 自检安装链路。

## 五、风险与控制

- 风险 1：技能参数变更导致上游调用断裂。
  - 控制：保留 ARK 兼容参数层，新增回归测试覆盖旧参数。
- 风险 2：双实现并存。
  - 控制：迁移完成即删除 ARK 旧实现，并做残留扫描。
- 风险 3：文档滞后。
  - 控制：每轮变更同步更新 `analyse` 与 `task` 文档。

## 六、完成定义（DoD）

- `academicresearch_kit/skills/*.py` 全量技能均通过 AOK 事务能力执行（直接事务调用或事务 execute 调用）。
- ARK `domains` 不新增实现。
- `tests/` 中具备对应 smoke 回归并通过。
- 文档与代码状态一致，未出现“文档已迁移、代码未迁移”偏差。

## 七、本轮进展（20260311）

- P0 三项已完成首轮收口：
  - `method_whitelist_selection`
  - `integrity_check`
  - `rag_evidence_synthesis`
- 已完成动作：
  - AOK 新增 3 个事务目录与标准三件套（`affair.py/json/md` + `__init__.py`）。
  - ARK 新增 3 个 `api` 门面并切换对应 `skills` 调用路径。
  - 新增 3 条 smoke 回归用例，覆盖门面调用链路。
  - 新增 3 个按文件夹组织的 workflow 示例（`demos/workflows/workflow_*/`）。
