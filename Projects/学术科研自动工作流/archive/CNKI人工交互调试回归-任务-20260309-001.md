# CNKI人工交互调试回归-任务-20260309-001

## 1. 需求与目标

针对“真实 CNKI 会话的详情抽取和真实导出最后一跳”，本任务目标是：

1. 若缺少交互调试能力，则先补齐可执行脚本。
2. 让人工完成安全验证后，可以直接串行回归：检索 -> 详情 -> 导出。
3. 输出明确的交互调试步骤。

## 2. 可行性分析

当前代码已支持：

1. `browser_config.cdp_url`（可附着外部浏览器会话）。
2. `execute=True` 的真实执行入口。

因此最小可行方案是新增一个脚本：

1. 启动带 `remote-debugging-port` 的 Edge。
2. 提示人工登录与安全验证。
3. 复用该会话执行 `cnki-search`、`cnki-paper-detail`、`cnki-export`。

## 3. 计划步骤

1. 新增 `scripts/cnki_interactive_debug.py`。
2. 脚本参数支持：查询词、CDP 端口、profile、导出模式、Zotero 开关。
3. 用 `.venv` 做 `--help` 烟雾验证。

## 4. 实施记录

已完成：

1. 新增 `scripts/cnki_interactive_debug.py`。
2. 脚本会自动：
   - 启动 Edge（CDP 模式）
   - 等待人工过验证
   - 执行真实检索并可交互选择条目
   - 执行真实详情抽取
   - 执行真实导出并落盘
3. 已完成语法检查与 CLI 帮助验证，脚本可直接运行。

## 5. 当前进度与结果

本任务已完成“交互调试能力补齐”。

当前仓库已经具备：

1. 代码侧真实执行能力。
2. 人工验证后继续回归的会话复用能力。
3. 可直接执行的单命令调试入口。

## 6. 下一步建议

1. 用真实账号与校园网权限执行一次完整回归。
2. 保留同一个 profile 进行二次回归，验证稳定性。
3. 回归完成后再补充一份操作手册节选到用户文档。
