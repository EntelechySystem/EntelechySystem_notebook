# 学术事务调度与三库开发实施-任务-20260307-010

## 1. 本轮目标

1. 将工作区 Python 解释器显式收口到项目 `.venv`。
2. 减少 `academicresearch_kit` 与 `academicresearch_kit.skills` 在编辑器中的静态导入误报。
3. 记录解释器收口、分析配置补强与验证结果。

## 2. 可行性分析

1. 当前仓库根目录已存在 `.venv/`，并且此前所有关键脚本均通过 `./.venv/Scripts/python.exe` 验证成功。
2. 编辑器层误报的核心信号是：Python 环境工具当前识别到系统 `C:/Python314/python.exe`，而非仓库内 `.venv`。
3. 仓库当前尚无 `.vscode/settings.json`，适合直接新增工作区级设置，显式指定默认解释器、终端激活与分析路径。
4. 已存在 `pyrightconfig.json`，可继续补充 venv 相关字段，与 `.vscode/settings.json` 形成双层收口。

## 3. 实施计划

1. 新增 `.vscode/settings.json`，强制工作区默认解释器指向 `.venv`。
2. 更新 `pyrightconfig.json`，补充 venv 解析字段。
3. 对关键脚本再次执行 Problems 检查，观察误报收敛情况。
4. 回填本轮结论与下一步建议。

## 4. 实施进度

1. 已创建本轮任务文档。
2. 已确认仓库根目录存在 `.venv/`，其中包含 `Scripts/`、`Lib/` 与 `pyvenv.cfg`。
3. 已新增 `.vscode/settings.json`，将 `python.defaultInterpreterPath` 显式指向 `${workspaceFolder}\\.venv\\Scripts\\python.exe`，并开启终端环境激活、工作区分析与额外搜索路径。
4. 已更新 `pyrightconfig.json`，补充 `venvPath` 与 `venv` 字段，使静态分析也优先解析仓库内 `.venv`。
5. 已对关键脚本再次执行 Problems 检查，`academicresearch_kit/skills/__init__.py`、`.claude/skills/ark-workflow-executor/scripts/run_ark_workflow.py`、`.claude/skills/api-data-fetch/scripts/run_api_data_fetch.py`、`.claude/skills/project-bootstrap/scripts/run_project_bootstrap.py` 当前均显示 `No errors found`。
6. 本轮结论：解释器与分析配置已完成双层收口，残余风险已从“代码结构错误”收敛为“若编辑器仍显示旧问题，更可能是索引缓存未刷新”。

## 5. 验收口径

1. 工作区设置中存在明确的 `.venv` 解释器路径。
2. Python 分析配置不再仅依赖系统解释器默认发现逻辑。
3. 若仍有残余误报，需能明确定位为编辑器缓存或索引状态，而非仓库代码结构错误。

## 6. 本轮结果与建议

1. 本轮验收已通过：工作区级设置与 pyright 配置均已落地，且旧误报核心文件复查结果为无错误。
2. 后续若 VS Code 仍短暂展示历史 Problems，优先执行解释器重新选择或重载窗口，而不是继续改动导入结构。
3. 该项任务可以视为完成，后续仅在新增多根工作区或更换虚拟环境路径时再补配置。
