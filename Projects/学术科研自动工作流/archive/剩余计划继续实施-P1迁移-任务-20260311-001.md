# 剩余计划继续实施-P1迁移-任务-20260311-001

## 任务背景

根据 `plan-ARK技术层迁移剩余实施计划-20260311-001.md`，继续执行剩余计划中的 P1：
- `local_reference_ingestion`
- `knowledge_prescreen`
- `literature_reading`

目标是将上述 3 个领域能力从 ARK `domains/*` 迁移到 AOK 事务层，并保持 ARK skill 与包导出接口不变。

## 可行性分析

1. 代码耦合度中等、边界清晰。
- 3 个域引擎均为纯 Python 逻辑，输入输出契约明确，迁移风险可控。

2. 兼容策略可复用。
- 参考已完成的调度与 taskdb 迁移模式：ARK 侧保留门面层，AOK 侧承接真实实现。

3. 风险点可提前规避。
- 直接跨仓静态导入在 ARK 工作区会触发解析告警，需使用 API 门面动态解析。
- 删除 ARK 旧域目录前需先完成调用切换与全量检索，避免残留引用。

## 计划步骤

1. 在 AOK 新增 P1 三个事务模块与注册元数据。
2. 将 ARK skills 的实现调用切换到 AOK（经 ARK API 门面层）。
3. 更新 ARK 包级导出，确保外部导入路径稳定。
4. 删除 ARK 对应 3 个旧 `domains` 目录。
5. 执行静态错误检查与残留引用检索。
6. 同步 API 手册并记录本轮结果。

## 实施过程与结果

### 步骤 1：AOK 新增事务模块

已完成新增：
- `autodokit/affairs/本地文献导入/`
- `autodokit/affairs/知识预筛选/`
- `autodokit/affairs/文献阅读规划/`

每个事务均补齐：
- `__init__.py`
- `affair.py`（包含与 ARK 兼容的 Engine/Result 数据结构）
- `affair.json`
- `affair.md`

### 步骤 2-3：ARK 调用切换与导出保持

已完成：
- `academicresearch_kit/skills/local_reference_ingestion.py`
- `academicresearch_kit/skills/knowledge_prescreen.py`
- `academicresearch_kit/skills/literature_reading.py`
- `academicresearch_kit/__init__.py`

说明：
- 初次直接导入 `autodokit.affairs.*` 触发 ARK 静态解析告警。
- 已改为 ARK API 门面动态解析方案，新增：
  - `academicresearch_kit/api/local_reference_ingestion.py`
  - `academicresearch_kit/api/knowledge_prescreen.py`
  - `academicresearch_kit/api/literature_reading.py`

### 步骤 4：清理 ARK 旧域目录

已删除：
- `academicresearch_kit/domains/local_reference_ingestion/`
- `academicresearch_kit/domains/knowledge_prescreen/`
- `academicresearch_kit/domains/literature_reading/`

### 步骤 5：校验

已完成：
- 对新增/修改文件执行错误检查，关键文件无报错。
- 检索 `domains.(local_reference_ingestion|knowledge_prescreen|literature_reading)` 残留引用，未发现代码残留。

### 步骤 6：文档同步

已更新：
- `autodo-kit/docs/API手册.md`

新增 `1.7 新增事务能力（S5-P1）`，同步了三项新事务与兼容类导出说明。

## 进度总结

- 本轮目标（P1）已完成：AOK 承接 + ARK 门面切换 + 旧域删除 + 校验 + API 手册同步。
- 当前可进入 P2：`retrieval_governance` 迁移与 `api/retrieval.py` 切换。

## 下一步建议

1. 按剩余计划推进 P2：优先迁移 `retrieval_governance` 到 AOK affairs，并保持 ARK `api/retrieval.py` 契约不变。
2. P2 完成后执行一次端到端 smoke（至少覆盖 retrieval 路由主链和异常分支）。
3. 再进入 P3（CNKI 子域拆分迁移），最后统一做 P4 验收与文档收口。
