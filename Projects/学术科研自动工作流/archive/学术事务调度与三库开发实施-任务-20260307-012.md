# 学术事务调度与三库开发实施-任务-20260307-012

## 1. 本轮目标

1. 在 AI Agent 引擎接入阶段，暂时只围绕 `opencode` 落地 `academicresearch_kit` 的技能包装内容。
2. 停止本轮继续扩展 `.claude/skills`，改为优先补齐 `.opencode/skills` 对 `academicresearch_kit` 的调用脚本。
3. 修复 `academicresearch_kit/tools/project_runtime.py` 的静态类型报错。
4. 回填本轮 opencode 接入结果、验证过程与后续建议。

## 2. 可行性分析

1. 当前 `academicresearch_kit` 已经具备可复用的包内 skill 入口：`run_workflow_skill`、`run_project_bootstrap_skill`、`run_api_data_fetch_skill`、`run_model_routing_dispatch_skill`。
2. `.opencode/skills/` 已存在与这些能力对应的技能目录，其中 `project-bootstrap`、`api-data-fetch`、`model-routing-dispatch` 已有 `SKILL.md`，适合直接补脚本包装层。
3. `.opencode/skills/` 当前尚无 `ark-workflow-executor`，但该能力已在包内稳定存在，因此可以按相同模式新增 opencode skill 目录与脚本。
4. `project_runtime.py` 的报错来自 `summarize_workflow()` 对 `workflow.get(...)` 的类型收窄不足，属于低风险静态类型修复，不影响既有运行逻辑。

## 3. 实施计划

1. 修复 `academicresearch_kit/tools/project_runtime.py` 的类型收窄问题。
2. 为 `.opencode/skills/project-bootstrap`、`.opencode/skills/api-data-fetch` 补充 `scripts/*.py` 包装脚本，并在 `SKILL.md` 中补“当前接入方式”。
3. 复用既有模型路由脚本，补强 `.opencode/skills/model-routing-dispatch/SKILL.md` 的当前接入说明。
4. 新增 `.opencode/skills/ark-workflow-executor/`，提供 `SKILL.md` 与 `scripts/run_ark_workflow.py`。
5. 更新用户手册中的 skill 调用示例，使其改为 `.opencode` 路径。
6. 做错误检查与最小脚本验证，最后回填结果。

## 4. 实施进度

1. 已创建本轮任务文档。
2. 已定位 `project_runtime.py` 的 Problems：集中在 `len(affairs)`、`len(flow)` 与 `affair_keys` 的类型推断。
3. 已修复 `academicresearch_kit/tools/project_runtime.py`：在 `summarize_workflow()` 中补充 `raw_affairs`、`raw_flow` 的显式类型收窄，当前该文件 Problems 检查为 `No errors found`。
4. 已为 `.opencode/skills/project-bootstrap`、`.opencode/skills/api-data-fetch` 新增 `scripts/*.py` 包装脚本，并在各自 `SKILL.md` 中补充“当前接入方式”与调用示例。
5. 已新增 `.opencode/skills/ark-workflow-executor/`，包含 `SKILL.md` 与 `scripts/run_ark_workflow.py`，直接复用 `academicresearch_kit.run_workflow_skill()`。
6. 已将 `.opencode/skills/model-routing-dispatch/scripts/bailian_model_router.py` 从独立实现收口为薄包装脚本，改为调用 `academicresearch_kit.run_model_routing_dispatch_skill()`；同时补强 `SKILL.md` 中的当前接入说明。
7. 已更新 `docs/用户手册.md` 中的 Agent/Skill 调用示例，使其优先指向 `.opencode/skills/ark-workflow-executor/scripts/run_ark_workflow.py`。
8. 已更新 `pyrightconfig.json`，将 `.opencode/skills` 纳入静态分析范围，当前四个 opencode 包装脚本与配置文件均为 `No errors found`。
9. 已完成最小执行验证：
	- `ark-workflow-executor`：`workflow_count = 1`、`missing_workflow_count = 0`、`dry_run = true`。
	- `project-bootstrap`：成功初始化 `sandbox/opencode_skill_bootstrap_demo` 下的任务库、文献库、知识库与调度配置目录。
	- `api-data-fetch`：返回 `status = PASS`、`mode = retrieval-governance-probe`、`next_node = dataset_ingestion`。
	- `model-routing-dispatch`：返回 `status = PASS`、`mode = decision-only`、`primary_model = qwen3.5-plus`、`fallback_models = ["glm-4.7"]`，并成功写出 `sandbox/opencode_model_router_demo` 下的计划、运行日志与审计产物。

## 5. 验收口径

1. `project_runtime.py` 不再报告当前类型错误。
2. `.opencode/skills` 至少能直接调用 `academicresearch_kit` 的 workflow、project bootstrap、api data fetch 与 model routing 四项能力。
3. 用户手册中的 Agent/Skill 调用示例优先指向 `.opencode`。
4. 本轮文档明确说明当前阶段是 opencode-only，而非继续扩展 claude code 包装层。

## 6. 本轮结果与建议

1. 本轮验收已通过，当前 AI Agent 引擎接入已切换为 opencode 优先路线，`academicresearch_kit` 的四项核心可执行能力已能被 `.opencode/skills` 直接调用。
2. `project_runtime.py` 的报错已确认为静态类型收窄问题，并已修复，不涉及运行时逻辑回归。
3. 现阶段无需继续为 `.claude/skills` 做并行适配，后续应继续围绕 `.opencode/skills` 扩展剩余 skill，例如 `rag-evidence-synthesis`。
4. 下一步可优先执行两项工作：一是继续把已脚本化能力补入更多 `.opencode` skill；二是开始脚本化 `rag-evidence-synthesis` 的结构化抽取与证据矩阵生成。
